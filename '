import {defineConfig} from 'vite'

export default defineConfig({
  build: {
    minify: false,
    rollupOptions: {
      input: {
        entry: './src/index.ts',
      },
    },
  },
  resolve: {
    alias: {
      '@': './src',
    },
  }
})
