{
  "_comment": "The input code `entry.js` is expected to throw `ReferenceError: Cannot access 'bar' before initialization`",
  "expectExecuted": false,
  "config": {
    "input": [
      {
        "name": "entry",
        "import": "entry.js"
      }
    ]
  }
}