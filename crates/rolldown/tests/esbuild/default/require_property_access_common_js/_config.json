{
  "_comment": "Build output is executed in esm environment, `require` is not available",
  "expectExecuted": false,
  "config": {
    "format": "cjs",
    "input": [
      {
        "name": "entry",
        "import": "entry.js"
      }
    ]
  }
}
