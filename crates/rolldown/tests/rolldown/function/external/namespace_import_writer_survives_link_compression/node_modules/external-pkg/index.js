// A CommonJS module that fakes `__esModule` *and* owns a real `default`.
// Node's ESM/CJS interop ignores both and hands the raw `module.exports` to an
// ESM importer, so `__toESM(mod, 1)` and `__toESM(mod)` disagree here — which is
// what makes the node-mode flag observable.
module.exports = { __esModule: true, default: 'inner', marker: 'raw' };
