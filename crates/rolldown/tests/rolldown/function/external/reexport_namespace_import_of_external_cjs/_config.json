{
  "_comment": "https://github.com/rolldown/rolldown/issues/10069 - namespace variant. The re-export shim is tree-shaken away, but `ns.default` on the external namespace still needs the `__toESM` interop wrapper.",
  "config": {
    "external": ["node:https"],
    "format": "cjs"
  }
}
