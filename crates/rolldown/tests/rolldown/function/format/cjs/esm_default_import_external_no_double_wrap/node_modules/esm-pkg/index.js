// An ESM-only external with a default export (same shape as chalk@5).
// `require()` of this package goes through Node's require(esm) and returns
// a genuine module namespace.
export default function greet(name) {
  return `Hello, ${name}!`;
}
export const named = 'named export';
