{
  "config": {
    "format": "iife",
    "name": "1.2",
    "banner": "import assert from \"node:assert\"\nclass A {\n  constructor() {",
    "footer": "  }\n}\nlet a = new A();\nassert(a instanceof A);\nassert(typeof a[\"1\"][\"2\"] === \"object\");"
  }
}