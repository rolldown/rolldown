{
  "_comment": "A live two-argument import() is not rewritten by the module finalizer. If its wrapped dynamic entry is merged into the manually assigned host chunk, the entry facade must remain available at the original stable specifier so the import resolves and runs the entry trigger.",
  "snapshot": false,
  "config": {
    "input": [
      {
        "name": "a",
        "import": "./a.js"
      },
      {
        "name": "b",
        "import": "./b.js"
      }
    ],
    "entryFilenames": "[name].js",
    "chunkFilenames": "[name].js",
    "strictExecutionOrder": true,
    "experimental": {
      "onDemandWrapping": true
    },
    "codeSplitting": {
      "groups": [
        {
          "name": "dyn",
          "test": "[\\\\/](?:target|observer)\\.js$"
        }
      ]
    }
  }
}
