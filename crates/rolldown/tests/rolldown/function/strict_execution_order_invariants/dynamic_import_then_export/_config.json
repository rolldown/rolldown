{
  "_comment": "A wrapped dynamic entry shares its host chunk with an unrelated callable export named `then`. With internal export names preserved, dynamically importing that host would assimilate its namespace as a thenable before the generated extraction callback runs, so the target facade must remain the dynamic-import boundary.",
  "snapshot": false,
  "config": {
    "input": [
      {
        "name": "a",
        "import": "./a.js"
      },
      {
        "name": "b",
        "import": "./b.js"
      }
    ],
    "entryFilenames": "[name].js",
    "chunkFilenames": "[name].js",
    "minifyInternalExports": false,
    "strictExecutionOrder": true,
    "experimental": {
      "onDemandWrapping": true
    },
    "codeSplitting": {
      "groups": [
        {
          "name": "dyn",
          "test": "[\\\\/](?:target|observer|then-export)\\.js$"
        }
      ]
    }
  }
}
