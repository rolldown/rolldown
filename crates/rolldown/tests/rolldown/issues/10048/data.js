// Plain data (like storybook's chunk-IH6QJILI.js `themes`). Crucially, `themes`
// does NOT depend on the interop helpers. theming.js re-exports it.
export const themes = {
  light: { base: 'light', appBg: '#ffffff' },
  dark: { base: 'dark', appBg: '#000000' },
};
