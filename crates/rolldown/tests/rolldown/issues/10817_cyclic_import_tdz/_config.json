{
  "_comment": "A linked constant imported through a static cycle is not initialized merely because its canonical owner is known. The coercion in b.js must remain and throw while VALUE is in its TDZ.",
  "config": {
    "input": [{ "name": "main", "import": "./a.js" }],
    "minify": false
  },
  "snapshot": false
}
