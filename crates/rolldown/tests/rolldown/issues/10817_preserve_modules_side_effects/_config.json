{
  "_comment": "The imported-constant refinement must update module side effects before side-effect-only imports propagate. After rolldown removes the coercion in derived.js, preserveModules must emit neither the import nor an empty derived.js.",
  "config": {
    "input": [{ "name": "main", "import": "./main.js" }],
    "minify": false,
    "preserveModules": true
  },
  "snapshot": false
}
