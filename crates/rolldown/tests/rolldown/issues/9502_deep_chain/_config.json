{
  "config": {
    "input": [
      {
        "name": "tu",
        "import": "./test-utils.js"
      },
      {
        "name": "rt",
        "import": "./runtime-entry.js"
      }
    ],
    "platform": "browser",
    "format": "esm",
    "manualCodeSplitting": {
      "groups": [
        {
          "name": "shared",
          "test": "shared\\.js$"
        },
        {
          "name": "vue",
          "test": "vue\\.js$"
        },
        {
          "name": "runtime",
          "test": "runtime\\.js$"
        }
      ]
    }
  },
  "_comment": "Regression test for rolldown/rolldown#9502 with a deeper barrel chain: tu -> vue (unwrapped, own chunk) -> runtime (unwrapped, own chunk, side-effect imports a CJS marker) -> shared (wrapped, own chunk). A second entry (rt) directly imports runtime to keep it from merging into vue. Verifies the early-exit propagates correctly across multiple barrel hops."
}
