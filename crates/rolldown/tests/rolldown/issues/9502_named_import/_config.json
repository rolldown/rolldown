{
  "config": {
    "input": [
      {
        "name": "tu",
        "import": "./test-utils.js"
      }
    ],
    "platform": "browser",
    "format": "esm",
    "manualCodeSplitting": {
      "groups": [
        {
          "name": "shared",
          "test": "shared\\.js$"
        },
        {
          "name": "vue",
          "test": "vue\\.js$"
        }
      ]
    }
  },
  "_comment": "Regression test for rolldown/rolldown#9502 with a named import (not namespace) of a wrapped re-export through an unwrapped barrel in a separate chunk."
}
