{
  "config": {
    "input": [
      {
        "name": "tu",
        "import": "./test-utils.js"
      }
    ],
    "platform": "browser",
    "format": "esm",
    "manualCodeSplitting": {
      "groups": [
        {
          "name": "shared",
          "test": "shared\\.js$"
        },
        {
          "name": "vue",
          "test": "vue\\.js$"
        }
      ]
    }
  },
  "_comment": "Regression test for rolldown/rolldown#9502 with a side-effect-only import of an unwrapped barrel in a separate chunk. Verifies the wrapped descendant still gets initialized before the importer runs."
}
