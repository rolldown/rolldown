{
  "_comment": "`app.js` contains `export * from 'node:path'` with `node:path` external. Same shape as `already_loaded_external_star` but with a builtin: its exports must stay available to the dynamic importer. The runtime `__reExport` merge inside the extracted namespace keeps them available while `shared.js` is inlined.",
  "config": {
    "input": [
      {
        "name": "main",
        "import": "./main.js"
      }
    ],
    "external": ["node:path"]
  }
}
