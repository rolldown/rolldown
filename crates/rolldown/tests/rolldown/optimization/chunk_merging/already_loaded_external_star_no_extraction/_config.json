{
  "_comment": "`app.js` contains `export * from 'ext'` with `ext` external. We cannot inline multiple modules in the dynamic entry because we don't know if it has a .then export.",
  "config": {
    "input": [
      {
        "name": "main",
        "import": "./main.js"
      }
    ],
    "external": ["ext"]
  }
}
