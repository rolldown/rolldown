// A callable `then` export makes any namespace carrying it thenable: promise
// resolution assimilates it, so the chain settles with what `then` produces
// instead of the namespace itself.
export function then(resolve) {
  resolve({ hijacked: true });
}
