// Editing a module the client has not reported as executed must fall back to
// a full reload. The old silent skip lost the edit forever, because the
// module cache advances regardless.
{
  "config": {
    "experimental": {
      "devMode": {}
    }
  },
  "expectExecuted": false,
  "dev": {
    "unregisteredClient": true
  }
}
