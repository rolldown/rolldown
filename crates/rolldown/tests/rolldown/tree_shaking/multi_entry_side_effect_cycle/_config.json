{
  "_comment": "A dependency can have side effects through a cycle. Either entry must execute those effects, even without constant refinement.",
  "config": {
    "input": [
      { "name": "a", "import": "./a.js" },
      { "name": "main", "import": "./main.js" }
    ],
    "minify": false
  },
  "snapshot": false,
  "configVariants": [{ "preserveModules": true }]
}
