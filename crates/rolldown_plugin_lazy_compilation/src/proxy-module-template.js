// Never executed. Every `import()` of this id was rewritten to
// `__rolldown_runtime__.requestLazy(...)`, which asks the dev server for the real module.
// This exists only so the graph has a node for `compile_lazy_entry` to scan from.
export {};
