{
  "name": "@example/module-federation",
  "private": true,
  "type": "module",
  "scripts": {
    "build:host": "pnpm --filter @example/module-federation-host run build",
    "build:remote": "pnpm --filter @example/module-federation-remote run build",
    "build": "npm-run-all -l --parallel build:remote build:host"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
