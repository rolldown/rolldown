{
  "description": "Instrumented Vue transform matrix for startup, buildStart, handler, queue, payload, and concurrency attribution only.",
  "bindingProfile": "release",
  "cases": [
    {
      "name": "vue-icon-full-166-instrumented",
      "entrySet": "full",
      "instrumentation": true,
      "variants": ["ordinary", "worker-1", "worker-2", "worker-4", "worker-8"],
      "warmups": 1,
      "repeats": 3
    }
  ]
}
