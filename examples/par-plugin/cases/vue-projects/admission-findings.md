# Independent Vue admission findings

Status: untimed local admission on 2026-07-12. The lifecycle-corrected runtime triple is `b144106882fe244b19b738fc0acf3ffa07c7c9f3` / `7b8863bb28aefd2e2eb7409f8be6dae57a252fe4a2688383007be7ea2f847bf7` / `1efffd0b63483e77cd2854fe716941000ae9548768691d7b5a64dceb011f3c45`. These results make no wall-performance claim.

## Frozen project outcome

| Project         |                                                         Physical SFCs | Reached SFCs | Ordinary result                                                       | Worker smoke                                            |
| --------------- | --------------------------------------------------------------------: | -----------: | --------------------------------------------------------------------- | ------------------------------------------------------- |
| Floating Vue    |                                                                     4 |            4 | accepted, deterministic twice                                         | worker 1 and 4 exact parity                             |
| cabinet-fe/icon |                                                                   166 |          166 | accepted, deterministic twice                                         | worker 1 and 4 exact parity                             |
| PrimeVue bridge |                                                                   279 |          275 | accepted, deterministic twice                                         | worker 1 and 4 exact parity                             |
| GitLab          |                                                                 2,620 |      not run | rejected before transform: dual compiler contract unavailable         | correctly skipped                                       |
| Vben fallback   | 680 repository-wide, 375 in the app-plus-workspace reachable envelope |          366 | graph completes deterministically but fails the frozen minimum of 512 | worker 1 and 4 preserve the same rejected graph exactly |

Floating Vue reaches 12,715 source bytes. Its transform manifest is `b17a5c182691cbbcf67ca5c821aaa3263f0718d62473a1c563d55bdc8f435b9c`; graph modules are 22 with hash `fe220f3de020ecdfb7ea31ced00ee2b8191b3347207c5643aa1f2325881383d4`; normalized code and map hashes are `887ff2df144595dc370fafc17d1ff0b99716ea7a133ac8027996f72ce339397b` and `839759442bf37eedb49eed194a92c7c5f2f49a3f1334c80048d29811ac178dfc`.

cabinet-fe/icon reaches 109,122 source bytes. Its transform manifest is `7a600957c1702036c634f8f82994f573275a568f36f17a12f07e374ffcea212c`; graph modules are 170 with hash `ebae425eb384de68e688ff1670a74551d99cc9659c90f4098a0e8f899ab4e58a`; normalized code and map hashes are `de6d36419231a9a1fba3d857609600cc61451c25c1c855668e96a4c62b6b5412` and `48ba5d0f1e658d9e3515e0fb9e96bb15f35c879ec8ee264207662c82d05770bc`. Three of six output chunks contain no retained map; that exact absence is stable in ordinary and worker output rather than silently filled.

PrimeVue's real `packages/primevue/src/index.js` entry reaches 275 of 279 physical package SFCs and 1,717,901 source bytes. The four unreached files are `chart/BaseChart.vue`, `chart/Chart.vue`, `editor/BaseEditor.vue`, and `editor/Editor.vue`; the pinned entry does not export Chart or Editor. The transform manifest is `d90f220e90c56c4c941b62f381e036a69d3af5170c863a0c2e27c6b785326406`; graph modules are 433 with hash `1ef3104ae262980a32c0cd37bc0c5d825cdcf65c8d7f70fa463a19d6e733841a`; code and map hashes are `40b685689e2f83e5d59ed31a5ce925abe5019a055868834d26850a2264ddfcbe` and `89e9648fb646e9b74fc8bd6611c9e0609b14a610c3e63e279b3b0a6ac49f06d2`. PrimeVue remains a workspace-resolution bridge, not a large-project result.

GitLab's pinned sparse checkout verifies 2,620 SFCs, 10,577,454 bytes, and manifest `c9291d4515ce33e1654fcc37b32809e5cab5c2a701d597530494472428967cd3`. Executing the pinned `generateEntries` implementation yields 363 page entries and 374 total production entries. The entry source hashes are `910e9827cf1e34b8ea53038b5508c1242656e5c2abf4d3d1b598ff713b3116c8` for `webpack.config.js`, `2bd590893e590146988c65412437c362c133e5d4a0d89d39bd64a15a7bc82551` for `webpack.helpers.js`, and `cc5be9aa6b728c5c1eb4c9a2eb1376fb3f7df862bc3203088223a96131544527` for `entry_points.js`. The compiler contract now separately verifies exact hashes for `package.json`, `webpack.config.js`, Vue version selection, infection helpers and plugin, context aliases, and all three migration compilers. The pin requires Vue 2.7.16 and `vue-template-compiler` 2.7.16 plus Vue 3 compat/compiler-sfc 3.5.34, loader 15.11.1 and aliased loader 17.4.2, custom-element settings, and infection propagation. Twenty-two explicit `?vue3` entry edges were found. The available transform-only adapter has one Vue 3.5.39 compiler instance and cannot preserve that contract, so error `GITLAB_DUAL_COMPILER_CONTRACT_UNAVAILABLE` is retained before any misleading build is run.

Vben proves the frozen fallback gate is structurally impossible. The selected app plus every workspace package contains at most 375 SFCs, below the required 512 before reachability is considered. The real `apps/web-antd/src/main.ts` graph reaches 366 SFCs and 1,002,193 bytes; relocation-independent transform hash `80b9132c43cb492bc1b7fbec960bddcb4587ead99c561cf771ecefd60df4ebb2`; graph modules 735 with hash `8507e951fa45a6293174727c568476429d86106fe03579bcff9427db20c37dbd`; code and map hashes `13cdd13bf0504b1a0bad9512f6bb6d4cfd75b53512e7ad4d49595656d2e6f566` and `a7a1150374ade8cef049da2b33d7c31674afab8bd0413458ee455e86a222a9c8`. The frozen independent large-project requirement therefore remains incomplete and needs a protocol amendment; PrimeVue cannot fill it.

The Vben relocation audit compared prepared roots of 126 and 123 bytes. Exactly 28 of 366 transformed SFCs differed before normalization; each difference was one unplugin-vue style-block import containing the absolute prepared-project root. The raw result length therefore differed by three bytes for each affected SFC and by 84 bytes in aggregate. Replacing each checkout's exact root produced identical code for all 366 results, while output code and map evidence was already identical. The canonical result record consequently derives both its byte length and SHA-256 from that root-replaced code; tests retain sensitivity to changes in JavaScript and style-block query or subpath content.

The five official Vben app entries do not solve the scale gap. Conservative script-import closures reach 366 for web-antd, 368 for web-antdv-next, 367 for web-ele, 370 for web-naive, and 366 for web-tdesign. Their union is only 469 SFCs and 1,192,340 bytes with manifest `ca70c30cc9b413268763d33e75d7214310de3ea1e3e8ae8e1b39e999fe52b80a`: 342 files occur in every app graph and 127 are app-exclusive. The web-antd closure exactly matches its completed ordinary transform count, and the closure conservatively retains type-only imports. This direction cannot meet 512 and should not become a multi-app timing build.

## Protocol amendment candidates

TDesign Vue Next `dd334e2dc06d8ab48d1b6ebc5e9d4f6de67b16a2` is rejected. `packages/components` contains 744 physical SFCs and 1,121,203 bytes with manifest `244bdbd8599dccea2032e7a088a1f22a0cea1301494455899af952f81a554b77`, but real library entry `packages/components/index.ts` reaches zero SFCs. It completes a 708-module ordinary graph with code hash `5bf10e568b213ea8c212e6fcef01aa8f22230875dfafe8184069928a5f95822e` and map hash `ec6ed4273e76bce045bf3b9bf8e095320c9e47e935ceccd22bfda000755ad05b`. Of the physical SFCs, 685 are examples, demos, or tests and 59 are `_usage` files. Official `internal/builds/vue-next/build-components.ts` SHA-256 `9b6407661ee68344ce7e4067b39de1f2b0a0be46f9f136009b777cbb13040275` uses `index-lib.ts` and multi-input `**/*.ts`/`**/*.tsx`, not Vue inputs; the only TS/TSX-to-Vue import in the subtree is an excluded test fixture. The production library build therefore also has zero Vue-transform reachability.

Directus `9f2f73aee7d8647d3f187dac43f724fe617763f5` is the first viable replacement candidate. The audited checkout contains 561 physical SFCs across the app and checked-out workspaces, 2,675,339 bytes, and manifest `9c790f915893da23aa1406a6e8744a74a71cea4841a377ac20c5d803947d12a7`. The real `app/src/main.ts` graph transforms 546 SFCs and 2,667,307 source bytes. This corrects the earlier 545 count: `packages/themes/src/components/theme-provider.vue` was always in the module graph but was omitted by an audit scope restricted to `app/src`. Every reached repository SFC is now audited. The corrected source manifest is `2fc2f91479509f3ec7e45fcfce527f07e87d840ddaaf1fb20e2fd2e5744176cf`, and the relocation-independent source-plus-result transform manifest is `94598da3c83602feeec361ccf9eba37ce77c7571dfe663c8d89c472a74a7602a`. Fifteen physical SFCs are outside the graph: the previous five app files and ten extension SDK template files. The final checked-out-workspace graph remains 1,966 modules with manifest `5a189112149b735fa33cc97b900303a873f3e908e87541f90d55aad87eafbabc`; output code and map hashes remain `7d39e88ca1f0a8a31cafe898e0a0e7fb7c6a92815aedca4460943618a343dc26` and `007636cd19156e4de84f5f1579559b97501d97e9c6467757f583d810f940882b`.

Directus dependency preparation invokes Corepack pnpm 10.27.0, verifies root lock hash `aeafd45f0650bd6265ada31e7a530f3a83ae62b7cce1c36eb60f45de9b2b42c1`, installed lock hash `560ff04b812fd10d7b6fa8a627164a4462c755f38fe848b490cf12923e54308a`, install metadata hash `80481dd64e389501ddf919b443d6635cafaa0a2d98fe307be1e661735b3117e4`, scripts off, project Vue 3.5.24, and project plugin-vue 6.0.1. The research adapter does not inherit that 3.5.24 compiler: it explicitly supplies adapter-local `vue/compiler-sfc` 3.5.39, including Vue's Node TypeScript registration wrapper. The root declares Node 22, while this study deliberately records Node 24.18.0. Six literal `import.meta.glob` expansions select 126 files with manifest `c0e6bf016a14745680bdc1ac4dcc71ff73a11c0c0248f559752a6bbd73086dcf`. Thirty-three checked-out workspace package declarations have manifest `62b3fe65a546773989df2e8fef0b0a3ac360e88af4ed921c66299009de73b168`; 480 actual workspace resolution edges have manifest `5ece9537182b9d3ecb6813c0c3780b732e38cee0942406e5645ee0f2db424612`. Catalog `@directus/license` and `@directus/vue-split-panel` stay external.

The corrected Directus golden uses the explicitly pinned 3.5.39 Node wrapper. A rejected intermediate attempt to require `@vue/compiler-sfc` directly exposed why the wrapper matters: imported-type resolution failed because `vue/compiler-sfc/register-ts.js` had not registered TypeScript filesystem access. A provisional untimed amendment matrix after the relocation fix reproduced two ordinary runs plus worker one/four exactly, including the corrected full-workspace audit and unchanged output code and map hashes. It is not durable evidence because the harness was necessarily dirty while validating the fix. After the harness is committed and the worktree is clean, the amendment matrix must regenerate and retain its raw artifact and canonical summary in the committed content-addressed evidence store. Directus remains a protocol-amendment candidate and has not been timed.

Across all projects, the complete installed package payloads for `unplugin-vue` 7.2.0, the Vue 3.5.39 wrapper, and `@vue/compiler-sfc` 3.5.39 are content-hashed together with their actual loaded entries, the Node binary, root and installed pnpm locks, modules metadata, and the three research adapter sources. Package-local `node_modules` directories are intentionally excluded because pnpm-generated command shims contain absolute checkout paths; the installed lock and modules metadata bind dependency resolution separately. The unplugin, wrapper, compiler, and adapter-source aggregate hashes are `92cc4139…`, `e1ba4d43…`, `47016522…`, and `44e5c652…`. Output map hashes are parity artifacts, not source-map correctness: the transform adapter supplies no map, so source positions remain untested. Ordinary-versus-worker diagnostic parity also remains untested because the matrix has no paired invalid-SFC or worker-failure case.

The Amendment 4 formal runner is implemented but has not executed a performance child. Its validate-only result correctly remains `performanceReady=false` until these sources are committed, both source/runtime worktrees are clean, and current small/medium plus corrected Directus correctness raw/compact artifacts are committed under their content address in `hyf0/rolldown-parallel-js-plugin`. The gate re-verifies those bytes against artifact-store `HEAD` and rechecks runtime, harness, package trees, prepared projects, matrix, golden, and evidence after execution. The canonical wall remains `/usr/bin/time -l` real time; the exact parent nanosecond span must contain it within at most 10 ms printed precision and may add no more than 250 ms. The later screen requires a qualifying local host for every child; confirmation also requires the exact screened raw and compact pair. Independent-project summaries report workload portability and small-case regression but cannot define a nested Vue crossover.
