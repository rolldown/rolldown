# Pass-Based Pipeline — Implementation

> The rationale and enforcement boundary behind this machinery live in [design.md](./design.md).

## Summary

Phase 0 provides a shared synchronous pass harness in `rolldown_utils`, runtime tests for execution and diagnostic behavior, and consuming-crate compile tests for the ownership contract. Phase 1 adds non-default link baseline hooks and a dedicated runner. Phase 2 begins production adoption with `ComputeTlaPass`. Phase 3 moves all traversal and aggregation out of `LinkStage::new`, adds the first real typed chain, and now carries canonical entries into serial format classification before splitting CJS namespace merges, dynamic exports, and wrapper planning into separate domains. The current driver starts with ten narrow passes; wrapper declaration allocation, lazy normalization, side effects, and later link work still use the legacy carrier during migration. This document records the machinery, current field lifecycles, and boundary rules for the continuing migration.

## Component map

| Component               | Location                                                                                                                      | Responsibility                                                                                                                                                                                                                                       |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Public module export    | `crates/rolldown_utils/src/lib.rs`                                                                                            | Exposes the shared `pass` module.                                                                                                                                                                                                                    |
| Harness                 | `crates/rolldown_utils/src/pass.rs`                                                                                           | Defines `Pass`, the branded execution token, raw result envelope, sealing wrapper, public result aliases, contexts, and execution entries.                                                                                                           |
| Runtime tests           | `crates/rolldown_utils/tests/pass_harness.rs`                                                                                 | Exercises sealed and owned outputs, typed error propagation, and deterministic branch diagnostic merging.                                                                                                                                            |
| Compile-test driver     | `crates/rolldown_utils/tests/pass_harness_ui.rs`                                                                              | Runs one valid code-generating case and all pass-harness compile-fail cases.                                                                                                                                                                         |
| Compile fixtures        | `crates/rolldown_utils/tests/ui/pass_harness/`                                                                                | Pins capability, privacy, lifetime, ownership, ordering, sealing, context, and codegen failures from a consuming crate.                                                                                                                              |
| Link baseline hooks     | `crates/rolldown/src/stages/link_stage/testing.rs`                                                                            | Measures the exact scan-outside/link-inside boundary and installs a Tokio task-local callback that observes the produced link output while the unchanged standard Generate path continues; available only through the non-default `testing` feature. |
| Baseline runner         | `crates/bench/src/link_baseline/` and `crates/bench/src/bin/link_baseline/`                                                   | Defines pinned workloads, timing and trace modes, exact output and diagnostic digests, process-RSS controls, fail-closed canonical environment validation, and the versioned JSON report.                                                            |
| Baseline tests          | `crates/bench/tests/link_baseline.rs` and `crates/bench/tests/link_trace_global.rs`                                           | Pin workload images, injective digest framing, timing statistics, stable-ID interval tracing, existing-Rayon-worker coverage, the core timing hook, same-run Generate observation, and mixed diagnostic order.                                       |
| Link passes             | `crates/rolldown/src/stages/link_stage/passes/`                                                                               | Forbids unsafe code and contains ten production passes, narrow artifacts, focused algorithm tests, and the non-vacuous AST source inventory.                                                                                                         |
| Typed entry/order chain | `canonicalize_entries.rs` and `compute_module_execution_order.rs`                                                             | Consumes raw entries into one owned draft, lends that draft to pure execution-order analysis, returns sealed dense orders and an owned sorted list, and preserves deterministic DFS behavior.                                                        |
| TLA pass                | `crates/rolldown/src/stages/link_stage/passes/compute_tla.rs`                                                                 | Reads only `ModuleTable`, consumes `TlaScanFacts`, emits require-TLA diagnostics through `PassCtx`, and returns sealed `TlaFacts` without changing the existing DFS behavior.                                                                        |
| Representation passes   | `determine_module_formats.rs`, `compute_cjs_namespace_merges.rs`, `compute_dynamic_exports.rs`, and `plan_module_wrapping.rs` | Preserve serial first-observer format semantics, split three downstream domains, seal dynamic-export facts, and consume wrapper seeds exactly once.                                                                                                  |

## Public API and control flow

The implemented shape is:

```text
driver-owned PassPipelineCtx
  └── run_pass / run_infallible_pass
        ├── assert concrete pass size is zero during code generation
        ├── enter the pass tracing span
        ├── create RunBrand and RunToken<'_, P>
        ├── derive temporary write-only PassCtx
        └── P::run(token, cx, read, owned)
              └── token.finish(read_output, owned_output)
                    └── private-field RawPassOutput
        └── harness opens RawPassOutput
              ├── Sealed<OutputRead>
              └── OutputOwned
```

`Pass` has `InputRead<'a>`, `InputOwned`, `OutputRead`, `OutputOwned`, and `Error` associated types. `InputRead<'a>` is `Copy`; all other associated types are `'static`. Its `run` method receives `RunToken<'_, Self>`, where the anonymous token lifetime is bound to that invocation. `PassOutput<P>` names `(Sealed<P::OutputRead>, P::OutputOwned)`, and `PassResult<P>` names the corresponding result with `P::Error`. `run_pass` returns `PassResult<P>` without erasing the concrete associated error. `run_infallible_pass` is restricted to `Error = Infallible`, returns `PassOutput<P>`, and removes the uninhabited error by exhaustive matching.

`RunToken<'run, P>` contains a private mutable borrow of a harness-local `RunBrand`, an invariant lifetime marker, and a pass-type marker. It is neither `Copy` nor `Clone`. Its only public operation, `finish`, consumes it and creates `RawPassOutput`; that envelope's fields are private, so only the harness can open it.

`Sealed<T>` has a private field and only implements shared `Deref`. It has no public constructor, mutable dereference, or consuming unwrap. The harness constructs it unconditionally around `OutputRead`, including `OutputRead = ()`.

## Context ownership and diagnostics

The driver owns `PassPipelineCtx`. A serial pipeline uses one context; concurrent branches use separate contexts and append them to the parent in declared pass order. `append` preserves the order already present in each branch. `into_diagnostics` consumes the context and returns the existing `Diagnostics` type.

Only the harness constructs `PassCtx<'_>`, and each value exists for one invocation. The pass can call `push` or `extend`; it cannot construct, inspect, drain, or retain the underlying pipeline context. The harness stores the emitting pass's concrete type name with each diagnostic. When the pipeline context is consumed, that provenance is emitted to the `rolldown::pass` tracing target before the ordinary diagnostic is returned.

The context is a sink, not a dependency container. Pipeline data must remain in the declared read and owned slots.

## Compile and runtime coverage

`pass_harness.rs` contains three runtime cases:

- An infallible pass returns a sealed read-side fact while its owned output continues into another pass.
- A fallible pass returns its own typed error unchanged.
- Two branch contexts appended in declared order yield diagnostics in that same order. A local recording subscriber also pins the `run_pass` span, `rolldown::pass` event target, and concrete pass provenance for each merged diagnostic.

`pass_harness_ui.rs` first compiles `pass_valid.rs`, then compiles every `fail_*.rs` fixture expecting failure. The passing case is required because trybuild otherwise may use a check-only path; the non-zero-sized pass assertion lives in an inline const inside `run_pass` and must be exercised by code generation.

The fixtures pin these boundaries:

- External code cannot construct `RunToken`, `RawPassOutput`, or `Sealed`, inspect the raw or sealed private fields, unwrap a sealed value, obtain mutable access through it, or read `PassCtx`.
- A caller cannot invoke `Pass::run` without a token, use a token for the wrong pass, or leak the branded token through the associated error, either output, a panic payload, or a spawned thread.
- `InputRead` cannot contain a direct or nested `&mut`, and one owned input cannot be moved into sibling branches.
- Wrong driver order and a literal `()` pattern for `Sealed<()>` fail.
- A non-zero-sized pass fails in the code-generating test.

These tests do not prove the absence of interior mutability, globals, broad artifacts, inappropriate `Clone` access, duplicate values placed in a different output channel, or semantic ordering dependencies. They also do not prevent a pass from naming `PassPipelineCtx` in one of its own slots. Those remain source-test and review responsibilities described in `design.md`.

## Link adoption boundary

The selected adoption is the internals created by `LinkStage::new` and driven by `LinkStage::link`. Its external interface remains unchanged: input is `NormalizedScanStageOutput` plus `SharedOptions`, and `.link()` remains infallible and returns `(LinkStageOutput, IndexEcmaAst, UsedSymbolRefsBuilder)`. Code outside link does not need to understand the pass migration.

`LinkStage` may remain as a one-shot facade that holds only the untouched scan output and options until `.link()` consumes it. It must not remain a persistent cross-pass carrier for derived state. No pass may accept `LinkStage`, `LinkStageOutput`, `LinkingMetadata`, `LinkingMetadataVec`, or a renamed equivalent.

The future driver has three parts:

```text
NormalizedScanStageOutput + SharedOptions
  └── input adapter: move fields, initialize empty values, seed diagnostics
        └── typed link driver: narrow artifacts and owned entity working sets
              └── legacy output adapter: consume final values once
                    └── existing mutable link output tuple
```

Neither adapter is a pass. The input adapter performs no traversal, sorting, aggregation, or link analysis. Passthrough fields remain ordinary driver locals rather than entering the pass DAG. The output adapter is the only place that reconstructs `LinkStageOutput` and legacy `LinkingMetadataVec`.

### Legacy output ownership policy

The output adapter must construct the mutable legacy boundary without weakening `Sealed<T>`:

| Value kind                              | Pass channel  | Driver behavior                                                                                           | Final action                                                                                       |
| --------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| Link-local fact read by later passes    | `OutputRead`  | Keep `Sealed<T>` and lend `&T` through dereference.                                                       | Drop after its last link consumer, or project a compact fact into one legacy field at the adapter. |
| Domain-final value required by Generate | `OutputOwned` | Keep ownership in a domain-specific final type, lend `&T` to readers, and do not expose general mutation. | Move it once into the legacy output adapter.                                                       |
| Mutable entity table or draft           | `OutputOwned` | Move it only into passes that mutate or consume it; unrelated passes receive narrow shared reads.         | Move it once into the legacy output tuple or drop it at its declared last use.                     |

There is no `Sealed::into_inner`, no adapter-only general unwrap, and no clone of a large table or map merely to cross the boundary. A value that must be moved into the legacy output stays on the owned channel. Its final domain type may restrict mutators with module privacy, but it is not described as mutation-sealed because Generate eventually receives a mutable legacy representation.

If a compact link-local sealed fact also has a legacy field, the adapter may copy or project that narrow fact and then drop the sealed artifact. This is the temporary policy for facts such as TLA reachability during migration. A projection must name its removal phase; it must not become a second persistent representation or a way to pass legacy metadata back into the DAG.

## Current link field lifecycles

These tables record the current transitional field flow and retain the pre-migration consumer evidence used to review each later pass contract. “Last consumer” means the last semantic reader; it intentionally excludes the blanket end-of-link `Debug` dump of `LinkingMetadataVec`. That dump currently observes every metadata field and therefore keeps every value alive in an observational sense. Phase 5 must replace it with producer- or artifact-scoped tracing before early-drop claims become true.

### `LinkStage` and boundary fields

| Current carrier field                   | Producer or initialization                                                                                                                                 | Link reads and writes                                                                                                                                                             | Last link writer                  | Last semantic link reader or boundary action                                   |
| --------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------ |
| `module_table`                          | Scan; `ExtractGlobalConstantsPass` owns it briefly and removes `constant_export_map` values when enabled                                                   | Read by the ten typed passes and remaining legacy steps; temporary execution-order and representation projections plus lazy JSON, side-effect, reference, and inclusion mutations | `include_statements`              | `patch_module_dependencies`; move to `LinkStageOutput`                         |
| `entry_points`                          | Raw scan entries moved without traversal by `LinkStage::new`                                                                                               | Owned and consumed only by `CanonicalizeEntriesPass`                                                                                                                              | None                              | Replaced by the unique `EntryPlanDraft`; no duplicate remains                  |
| `entries`                               | Empty default; `CanonicalizeEntriesPass` produces `EntryPlanDraft`, which is consumed into this compatibility projection after format and wrapper planning | Read by synthetic exports, optimization, inclusion, and dependency finalization; rebuilt by inclusion                                                                             | `include_statements`              | `patch_module_dependencies`; move outward                                      |
| `symbols`                               | Scan                                                                                                                                                       | Mutated by wrapping, lazy JSON symbol-table rebuilding, binding, and reference analysis; then read by optimization, inclusion, and dependencies                                   | `reference_needed_symbols`        | `patch_module_dependencies`; move outward                                      |
| `depended_runtime_helper`               | Empty per-module side table in `LinkStage::new`                                                                                                            | Written by reference analysis; read and summarized by inclusion                                                                                                                   | `reference_needed_symbols`        | `include_statements`; drop as a link-only temporary                            |
| `stmt_infos`                            | Scan                                                                                                                                                       | Mutated by wrapping, lazy export, synthetic exports, reference analysis, and optimization; read by binding, inclusion, and dependencies                                           | `cross_module_optimization`       | `patch_module_dependencies`; move outward                                      |
| `runtime`                               | Scan                                                                                                                                                       | Read-only fact used by ordering, wrapping, synthetic exports, reference analysis, inclusion, and dependencies                                                                     | Scan                              | `patch_module_dependencies`; move outward unchanged                            |
| `sorted_modules`                        | Empty default; owned `SortedModules` from `ComputeModuleExecutionOrderPass` moves into the compatibility projection                                        | Read by cross-module optimization                                                                                                                                                 | `ComputeModuleExecutionOrderPass` | `cross_module_optimization`; move outward                                      |
| `metas`                                 | Empty per-module defaults in `LinkStage::new`; dependency, external-star, execution, and TLA facts are projected by the driver                             | See the per-field table below                                                                                                                                                     | `patch_module_dependencies`       | Move outward; replace the blanket aggregate trace                              |
| `diagnostics`                           | Seeded from scan warnings                                                                                                                                  | Appended by execution order, TLA, and binding                                                                                                                                     | `bind_imports_and_exports`        | Move outward                                                                   |
| `ast_table`                             | Scan                                                                                                                                                       | Mutated by lazy export; read by optimization                                                                                                                                      | `generate_lazy_export`            | `cross_module_optimization`; return as the separate tuple element              |
| `options`                               | Caller-owned shared borrow                                                                                                                                 | Read by multiple link steps                                                                                                                                                       | None                              | `patch_module_dependencies`; end the borrow                                    |
| `used_symbol_refs`                      | Empty in `LinkStage::new`                                                                                                                                  | Produced by inclusion                                                                                                                                                             | `include_statements`              | Return as the separate Generate continuation                                   |
| `used_external_symbols`                 | Empty in `LinkStage::new`                                                                                                                                  | Produced by inclusion                                                                                                                                                             | `include_statements`              | Move outward; Generate may extend it                                           |
| `safely_merge_cjs_ns_map`               | Empty in `LinkStage::new`; owned `CjsNamespaceMerges` is projected after its typed production                                                              | Read by reference analysis                                                                                                                                                        | `ComputeCjsNamespaceMergesPass`   | `reference_needed_symbols`; move outward                                       |
| `dynamic_import_exports_usage_map`      | Scan                                                                                                                                                       | Read by synthetic exports and inclusion; unchanged by link                                                                                                                        | None                              | `include_statements`; move outward unchanged as read-through data              |
| `normal_symbol_exports_chain_map`       | Empty in `LinkStage::new`                                                                                                                                  | Produced by binding; read by inclusion                                                                                                                                            | `bind_imports_and_exports`        | `include_statements`; move outward                                             |
| `external_import_namespace_merger`      | Empty in `LinkStage::new`                                                                                                                                  | Produced by binding; no later link reader                                                                                                                                         | `bind_imports_and_exports`        | Move outward                                                                   |
| `overrode_preserve_entry_signature_map` | Scan                                                                                                                                                       | Read by synthetic exports; unchanged by link                                                                                                                                      | None                              | `create_exports_for_ecma_modules`; move outward unchanged as read-through data |
| `entry_point_to_reference_ids`          | Scan                                                                                                                                                       | **Pure passthrough:** no link read or write                                                                                                                                       | None                              | Keep outside the DAG and move directly to the output adapter                   |
| `global_constant_symbol_map`            | Empty default; `GlobalConstantsDraft` from `ExtractGlobalConstantsPass` moves into this compatibility projection                                           | Binding removes invalid CJS constants; optimization adds or finds constants; inclusion reads them                                                                                 | `cross_module_optimization`       | `include_statements`; move outward                                             |
| `flat_options`                          | Scan                                                                                                                                                       | Read only by cross-module optimization                                                                                                                                            | None                              | `cross_module_optimization`; drop                                              |
| `user_defined_entry_modules`            | Scan                                                                                                                                                       | **Pure passthrough:** no link read or write                                                                                                                                       | None                              | Keep outside the DAG and move directly to the output adapter                   |
| `tla_scan_facts`                        | `LinkStage::new` groups scan's `tla_module_count` and `tla_keyword_span_map` without traversal                                                             | Moved as `ComputeTlaPass::InputOwned`; the zero-count path allocates no per-module output storage                                                                                 | None                              | `ComputeTlaPass`; consumed and dropped during the pass                         |
| `has_enum_inlining`                     | False in `LinkStage::new`                                                                                                                                  | Computed by inclusion                                                                                                                                                             | `include_statements`              | Move outward                                                                   |

`retained_export_symbols` is initialized empty by the output adapter and filled by Generate. It is neither a link product nor a scan passthrough.

### `LinkingMetadata`

| Metadata field                              | Producer or last link writer                                                                                                                                                                        | Last semantic link consumer                                                                                        | Boundary treatment                                                                                                                                                                                    |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `wrapper_ref`                               | Wrapping; lazy JSON may clear it                                                                                                                                                                    | Inclusion                                                                                                          | Project or move for Generate                                                                                                                                                                          |
| `wrapper_stmt_info`                         | Wrapping; lazy JSON may clear it after rebuilding statement identities                                                                                                                              | No direct field reader in link; the referenced statement participates in generic inclusion and dependency analysis | Keep the wrapper declaration identity paired with `wrapper_ref`; the adapter projects it into legacy metadata and Generate reads it against the post-replay inclusion bits during module finalization |
| `wrap_kind`                                 | Format and wrapping; lazy JSON finalizes it                                                                                                                                                         | Inclusion                                                                                                          | Project for Generate                                                                                                                                                                                  |
| `resolved_exports`                          | Binding                                                                                                                                                                                             | Inclusion                                                                                                          | Move or project for Generate                                                                                                                                                                          |
| `sorted_and_non_ambiguous_resolved_exports` | Binding                                                                                                                                                                                             | Synthetic exports                                                                                                  | Move or project for Generate                                                                                                                                                                          |
| `has_dynamic_exports`                       | Wrapping                                                                                                                                                                                            | Reference analysis                                                                                                 | Project for Generate; Generate later propagates it after on-demand changes                                                                                                                            |
| `shimmed_missing_exports`                   | Binding                                                                                                                                                                                             | Synthetic exports                                                                                                  | Move or project for Generate                                                                                                                                                                          |
| `required_by_other_module`                  | Wrapping                                                                                                                                                                                            | Wrapping                                                                                                           | Project for Generate on-demand wrapping                                                                                                                                                               |
| `referenced_symbols_by_entry_point_chunk`   | Synthetic exports                                                                                                                                                                                   | Dependency finalization                                                                                            | Move or project for Generate inclusion replay                                                                                                                                                         |
| `dependencies`                              | `CollectInitialDependenciesPass` produces `ModuleDependenciesDraft`; the driver immediately moves each slot into legacy metadata; binding extends it and dependency finalization is the last writer | Dependency finalization                                                                                            | Move or project for Generate; the initial draft never overlaps execution ordering or TLA                                                                                                              |
| `load_dependencies`                         | Dependency finalization                                                                                                                                                                             | No later link reader                                                                                               | Move or project for Generate                                                                                                                                                                          |
| `resolved_member_expr_refs`                 | Binding and member resolution                                                                                                                                                                       | Dependency finalization                                                                                            | Move or project for Generate                                                                                                                                                                          |
| `star_exports_from_external_modules`        | `CollectExternalStarExportsPass` produces `ExternalStarExports`; the driver immediately moves each slot into legacy metadata                                                                        | Synthetic exports                                                                                                  | Project for Generate; the owned collection artifact dies before execution ordering                                                                                                                    |
| `is_tla_or_contains_tla_dependency`         | Sealed `TlaFacts` from `ComputeTlaPass`                                                                                                                                                             | No later link reader                                                                                               | Phase 2 immediately projects only set bits into freshly default legacy metadata and drops `TlaFacts`; Phase 5 moves this projection into the final output adapter                                     |
| `concatenated_wrapped_module_kind`          | No link producer                                                                                                                                                                                    | None                                                                                                               | Generate-owned default initialized only in the output adapter                                                                                                                                         |
| `named_import_to_cjs_module`                | Binding                                                                                                                                                                                             | Member resolution and constant invalidation within binding                                                         | Drop immediately afterward; do not retain it in legacy metadata                                                                                                                                       |
| `import_record_ns_to_cjs_module`            | Binding                                                                                                                                                                                             | Inclusion                                                                                                          | Keep for Generate’s legacy inclusion replay until that replay is migrated                                                                                                                             |
| `included_commonjs_export_symbol`           | Binding                                                                                                                                                                                             | Inclusion                                                                                                          | Keep for Generate’s legacy inclusion replay until that replay is migrated                                                                                                                             |
| `depended_runtime_helper`                   | Inclusion summary; dependency finalization is the last writer                                                                                                                                       | Dependency finalization                                                                                            | Move or project for Generate                                                                                                                                                                          |
| `has_side_effectful_runtime_dep`            | Dependency finalization                                                                                                                                                                             | No later link reader                                                                                               | Move or project for Generate                                                                                                                                                                          |
| `module_namespace_included_reason`          | Inclusion                                                                                                                                                                                           | No later link reader                                                                                               | Move or project; Generate replay may update it                                                                                                                                                        |
| `namespace_included`                        | No link producer                                                                                                                                                                                    | None                                                                                                               | Generate-owned default initialized only in the output adapter                                                                                                                                         |
| `stmt_info_included`                        | Inclusion                                                                                                                                                                                           | Dependency finalization                                                                                            | Move or project; Generate may mutate it during replay and the runtime sweep                                                                                                                           |
| `is_included`                               | Inclusion                                                                                                                                                                                           | Dependency finalization                                                                                            | Move or project; Generate may mutate it during replay and the runtime sweep                                                                                                                           |
| `init_is_noop`                              | No link producer                                                                                                                                                                                    | None                                                                                                               | Generate-owned default initialized only in the output adapter                                                                                                                                         |
| `transitive_esm_init_targets`               | No link producer                                                                                                                                                                                    | None                                                                                                               | Generate-owned default initialized only in the output adapter                                                                                                                                         |

`wrapper_stmt_info` is an index into `IndexStmtInfos[module_idx]`, not a cached inclusion result. Lazy JSON normalization is its current identity invalidator and clears the index together with `wrapper_ref` and `wrap_kind` after rebuilding that module's statements. Generate can replay inclusion after link, so the pipeline cannot replace the index with a link-time boolean. The finalized wrapper declaration identity must cross the legacy boundary until Generate's finalizer performs its direct last read.

### Embedded facts and deferred module patches

| Embedded field or mutation                                                             | Producer or last writer                                                                                                                 | Last link consumer                                                                                                       | Planned treatment                                                                                                                                                                                      |
| -------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Module::exec_order`                                                                   | Sealed `ModuleExecutionOrders` from `ComputeModuleExecutionOrderPass`; the driver temporarily projects only assigned non-sentinel slots | Binding reads it for deterministic external-facade naming                                                                | Move the projection to the final adapter after binding reads the typed artifact directly                                                                                                               |
| `NormalModule::exports_kind`                                                           | Scan draft; format classification; lazy JSON is the last writer                                                                         | Inclusion                                                                                                                | Keep in `ModuleFormatsDraft → ModuleFormats`; project at the adapter                                                                                                                                   |
| `wrap_kind`, `wrapper_ref`, `wrapper_stmt_info`                                        | Format seeds, wrapping plan and declaration allocation; lazy JSON clears all three when it rebuilds the module's statements             | Inclusion reads wrapper kind and symbol; Generate finalization directly reads the statement index after inclusion replay | Transition through `WrapperSeeds → WrapperPlan → WrapperDeclarationsDraft → ModuleWrappers`; `StmtInfoIdx` is owned by `IndexStmtInfos[module_idx]`, and the adapter projects only the finalized state |
| `Module::side_effects`                                                                 | Scan draft; side-effect analysis finalizes it                                                                                           | Dependency finalization                                                                                                  | Produce `ModuleSideEffects`; project at the adapter                                                                                                                                                    |
| `NormalModule::constant_export_map`                                                    | Scan; consumed by `ExtractGlobalConstantsPass` when inline constants are enabled                                                        | Constant extraction itself                                                                                               | Move into `GlobalConstantsDraft`; disabled mode preserves the per-module maps, matching existing behavior                                                                                              |
| `ImportRecord::meta::CallRuntimeRequire`                                               | Reference analysis                                                                                                                      | No later link reader                                                                                                     | Emit `ReferenceImportRecordPatches` and apply once at the adapter                                                                                                                                      |
| `ImportRecord::meta::DeadDynamicImport`                                                | Inclusion                                                                                                                               | No later link reader                                                                                                     | Emit `TreeShakeModulePatches` and apply once at the adapter                                                                                                                                            |
| `EcmaView::json_module_none_self_reference_included_symbol`                            | Inclusion                                                                                                                               | No later link reader                                                                                                     | Emit `TreeShakeModulePatches` and apply once at the adapter                                                                                                                                            |
| Lazy JSON `named_exports`, `ast_usage`, AST, local symbol DB, and statement rebuilding | Lazy normalization                                                                                                                      | Binding, reference analysis, or inclusion depending on the field                                                         | Keep together in the explicit one-call owned lazy-normalization manifest; this operation invalidates old `SymbolRef` and `StmtInfoIdx` identities                                                      |
| `StmtInfo::{declared_symbols, referenced_symbols, eval_flags, import_records, meta}`   | Scan plus wrapping, lazy, synthetic, reference, and optimization writers                                                                | Inclusion and dependency finalization                                                                                    | Keep in owned `IndexStmtInfos`; do not repackage it as a broad read artifact                                                                                                                           |

## Pass module conventions for link

The link passes subtree declares `#![forbid(unsafe_code)]`. Each pass token is a non-generic unit struct named `XxxPass`; runtime configuration belongs in its slot manifest. The harness's code-generation size assertion remains a backstop, not a source-shape checker.

The non-vacuous `inventory` test parses every Rust file below the subtree, including local items. It pairs every pass declaration with exactly one unqualified `Pass` implementation; rejects generic, tuple, braced, qualified, renamed, local-hidden, macro-generated, and unmatched shapes; bans glob imports that could hide the trait; and rejects `LinkStage`, `LinkStageOutput`, `LinkingMetadata`, `LinkingMetadataVec`, and `PassPipelineCtx` paths in production pass code. Production attributes use a closed allowlist of documentation and the built-in `Clone`, `Copy`, `Debug`, and `Default` derives. The sole conditional exception is the exact private `#[cfg(target_family = "wasm")] use rolldown_utils::rayon::IteratorExt as _;` import required for the serial WASM shim; the inventory checks the target, full path, rename, visibility, and absence of a leading `::`, and rejection fixtures pin nearby variants. Production macro calls use a closed list of exact standard or dependency paths, type macros and nested macro calls are rejected, and every allowed macro argument is recursively scanned for forbidden carrier names. Focused rejection tests pin these cases. This remains a source-test boundary, so review must extend it if Rust syntax or the pass conventions change.

## Current production chain

`LinkStage::new` now performs only field moves and default allocation for the migrated initialization domains. `LinkStage::link` creates one `PassPipelineCtx` and starts with this statically visible serial chain:

```text
ExtractGlobalConstantsPass
  → CanonicalizeEntriesPass
  → CollectInitialDependenciesPass
  → CollectExternalStarExportsPass
  → ComputeModuleExecutionOrderPass
  → ComputeTlaPass
  → DetermineModuleFormatsPass
  → ComputeCjsNamespaceMergesPass
  → ComputeDynamicExportsPass
  → PlanModuleWrappingPass
```

| Pass                              | Read slot                                                                                      | Owned slot        | Read-side result               | Owned result and last use                                                                                                                                                                                                     |
| --------------------------------- | ---------------------------------------------------------------------------------------------- | ----------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ExtractGlobalConstantsPass`      | `ConstantExtractionInput { enabled }`                                                          | `ModuleTable`     | `()`                           | Returns the same table plus `GlobalConstantsDraft`; the driver immediately moves the draft into the current legacy constants field.                                                                                           |
| `CanonicalizeEntriesPass`         | `&ModuleTable`                                                                                 | `Vec<EntryPoint>` | `()`                           | Uniquely produces `EntryPlanDraft`. It preserves user entry order, canonicalizes the discovered suffix, and exposes only root iteration plus a consuming legacy conversion.                                                   |
| `CollectInitialDependenciesPass`  | `&ModuleTable`                                                                                 | `()`              | `()`                           | Produces `ModuleDependenciesDraft`; the driver moves every slot into legacy metadata immediately, before creating the next O(E) artifact. It excludes exactly dynamic-import and require records.                             |
| `CollectExternalStarExportsPass`  | `&ModuleTable`                                                                                 | `()`              | `()`                           | Produces `ExternalStarExports`; the driver moves every slot immediately and drops the artifact before execution ordering.                                                                                                     |
| `ComputeModuleExecutionOrderPass` | `ComputeModuleExecutionOrderInput<'_>` borrowing `ModuleTable` and the unique `EntryPlanDraft` | `()`              | Sealed `ModuleExecutionOrders` | Returns owned `SortedModules`. The pass is pure over its inputs; the driver projects assigned non-sentinel orders and moves the sorted list into its temporary legacy field, then releases the dense sealed table before TLA. |
| `ComputeTlaPass`                  | `&ModuleTable`                                                                                 | `TlaScanFacts`    | Sealed `TlaFacts`              | `()`; the driver projects only set bits and drops the compact fact immediately.                                                                                                                                               |
| `DetermineModuleFormatsPass`      | `DetermineModuleFormatsInput<'_>` borrowing `ModuleTable` and `EntryPlanDraft`                 | `()`              | `()`                           | Returns owned `ModuleFormatsDraft` and `WrapperSeeds`. The pass preserves physical module and import-record order and does not mutate its inputs.                                                                             |
| `ComputeCjsNamespaceMergesPass`   | `ComputeCjsNamespaceMergesInput<'_>` borrowing `ModuleTable` and `ModuleFormatsDraft`          | `()`              | `()`                           | Returns owned `CjsNamespaceMerges`, preserving namespace-reference order and the strict-execution-order disablement.                                                                                                          |
| `ComputeDynamicExportsPass`       | `ComputeDynamicExportsInput<'_>` borrowing `ModuleTable` and `ModuleFormatsDraft`              | `()`              | Sealed `DynamicExports`        | `()`; preserves the existing export-star DFS, cycle cache behavior, and dense module identity.                                                                                                                                |
| `PlanModuleWrappingPass`          | `PlanModuleWrappingInput<'_>` borrowing `ModuleTable` and `ModuleFormatsDraft`                 | `WrapperSeeds`    | `()`                           | Returns owned `WrapperPlan`; uniquely consumes seeds, preserves recursive propagation and strict/on-demand overrides, and carries `required_by_other_module` for every module slot.                                           |

This is the first production multi-consumer typed chain: `EntryPlanDraft` cannot be constructed or cloned by its consumers, is borrowed by execution ordering and format classification, and is moved into the temporary legacy entries field only after those borrows end. `ModuleFormatsDraft` then feeds three purpose-specific consumers, while `WrapperSeeds` has exactly one consuming edge into `PlanModuleWrappingPass`. The driver projects only after CJS namespace merging, dynamic-export analysis, and wrapper planning have completed. No pass receives `LinkStage` or legacy metadata.

The execution-order traversal preserves the old iterative DFS. Its cycle deduplication now uses first-discovery `FxIndexSet` order instead of nondeterministic `FxHashSet` iteration, making independent cycle warnings stable across fresh processes. The driver shares one serial pipeline context across execution ordering and TLA, drains it after both passes, and appends cycle warnings before require-TLA diagnostics exactly as the pass order declares.

### TLA contract

`ComputeTlaPass` has the following concrete contract:

| Slot            | Type              | Lifecycle                                                                                          |
| --------------- | ----------------- | -------------------------------------------------------------------------------------------------- |
| `InputRead<'a>` | `&'a ModuleTable` | Shared for this invocation only; module identity owns every `ModuleIdx` in the result.             |
| `InputOwned`    | `TlaScanFacts`    | Moves the direct-TLA count and keyword-span map into their only link consumer.                     |
| `OutputRead`    | `TlaFacts`        | Sealed by the harness; stores the module count and an `IndexBitSet<ModuleIdx>` of tainted modules. |
| `OutputOwned`   | `()`              | No mutable domain value continues.                                                                 |
| `Error`         | `Infallible`      | Eliminated exhaustively by `run_infallible_pass`; the external link signature stays infallible.    |

The direct-TLA count preserves the old constant-time common path: when it is zero, `TlaFacts` contains a zero-capacity bitset and neither allocates nor scans metadata. Otherwise, the moved DFS is unchanged, including its documented cycle memoization limitation, direct-edge preference in diagnostic chains, module and import-record iteration order, and release fallback for a missing keyword span.

The driver checks the artifact's module count, copies only set bits into freshly default metadata, and lets the sealed value leave scope before the next legacy link step. The generic consuming-crate `fail_mutate_sealed` fixture proves that the resulting `Sealed<TlaFacts>` cannot be mutated; the focused pass test additionally pins the real associated output type.

## Current phase status

- Phase 0 machinery exists in `rolldown_utils`: the branded token, private raw envelope, typed error channel, infallible entry, contexts, sealing wrapper, runtime tests, and compile fixtures are present.
- Phase 1 baseline code exists behind non-default features: the timing boundary, same-run diagnostic observer, deterministic synthetic and pinned real workloads, versioned reports, trace attribution, digest model, and scan/link process-RSS controls are present. Canonical measured results and budgets are recorded separately in `baseline.md` once accepted.
- Phase 2's first production slice exists: `ComputeTlaPass`, `TlaScanFacts`, sealed compact `TlaFacts`, the temporary legacy projection, focused algorithm tests, production trace coverage, and the link source inventory are implemented.
- Phase 3's initialization, execution-order, and representation-planning tranches exist: substantive `LinkStage::new` traversals and the first representation decisions are ten narrow passes; `EntryPlanDraft` and `ModuleFormatsDraft` have real typed consumers; wrapper seeds are consumed exactly once; execution order, TLA, and dynamic exports are sealed compact facts; and cycle diagnostics use deterministic first-discovery order.
- Wrapper declaration allocation, lazy normalization, final format/wrapper sealing, side effects, the remaining link domains, final input/output adapters, carrier removal, and measured whole-pass concurrency remain later work in this migration.

## Related

- [design.md](./design.md) — contract rationale, enforcement boundary, and rejected alternatives
- [baseline.md](./baseline.md) — reproducible link behavior, timing, digest, trace, and process-RSS contract
- `../linking/` — current link implementation notes and behavior constraints
