# Pass-Based Pipeline — Implementation

> The rationale and enforcement boundary behind this machinery live in [design.md](./design.md).

## Summary

Phase 0 provides a shared synchronous pass harness in `rolldown_utils`, runtime tests for execution and diagnostic behavior, and consuming-crate compile tests for the ownership contract. Phase 1 adds non-default link baseline hooks and a dedicated runner. Phases 2–4 move initialization, representation, binding, resolution, statement preparation, reference analysis, and cross-module optimization into typed passes. Phase 5 completes the Link cutover with `TreeShakePass`, `FinalizeModuleDependenciesPass`, and a one-shot legacy output adapter. The driver now runs twenty-four narrow passes; no production pass accepts `LinkStage`, legacy metadata, or the shared inclusion-core context, and `LinkStage` stores exactly the untouched scan output and options until `.link()` consumes it. Generate's public inclusion context and the external Link tuple remain unchanged.

## Component map

| Component                 | Location                                                                                                                                                                                     | Responsibility                                                                                                                                                                                                                                                                                |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Public module export      | `crates/rolldown_utils/src/lib.rs`                                                                                                                                                           | Exposes the shared `pass` module.                                                                                                                                                                                                                                                             |
| Harness                   | `crates/rolldown_utils/src/pass.rs`                                                                                                                                                          | Defines `Pass`, the branded execution token, raw result envelope, sealing wrapper, public result aliases, contexts, and execution entries.                                                                                                                                                    |
| Runtime tests             | `crates/rolldown_utils/tests/pass_harness.rs`                                                                                                                                                | Exercises sealed and owned outputs, typed error propagation, and deterministic branch diagnostic merging.                                                                                                                                                                                     |
| Compile-test driver       | `crates/rolldown_utils/tests/pass_harness_ui.rs`                                                                                                                                             | Runs one valid code-generating case and all pass-harness compile-fail cases.                                                                                                                                                                                                                  |
| Compile fixtures          | `crates/rolldown_utils/tests/ui/pass_harness/`                                                                                                                                               | Pins capability, privacy, lifetime, ownership, ordering, sealing, context, and codegen failures from a consuming crate.                                                                                                                                                                       |
| Link baseline hooks       | `crates/rolldown/src/stages/link_stage/testing.rs`                                                                                                                                           | Measures the exact scan-outside/link-inside boundary and installs a Tokio task-local callback that observes the produced link output while the unchanged standard Generate path continues; available only through the non-default `testing` feature.                                          |
| Baseline runner           | `crates/bench/src/link_baseline/` and `crates/bench/src/bin/link_baseline/`                                                                                                                  | Defines pinned workloads, timing and trace modes, exact output and diagnostic digests, process-RSS controls, fail-closed canonical environment validation, and the versioned JSON report.                                                                                                     |
| Baseline tests            | `crates/bench/tests/link_baseline.rs` and `crates/bench/tests/link_trace_global.rs`                                                                                                          | Pin workload images, injective digest framing, timing statistics, stable-ID interval tracing, existing-Rayon-worker coverage, the core timing hook, same-run Generate observation, and mixed diagnostic order.                                                                                |
| Link passes               | `crates/rolldown/src/stages/link_stage/passes/`                                                                                                                                              | Forbids unsafe code and contains twenty-four production passes, narrow artifacts, focused algorithm tests, and the non-vacuous AST source inventory.                                                                                                                                          |
| Typed entry/order chain   | `canonicalize_entries.rs` and `compute_module_execution_order.rs`                                                                                                                            | Consumes raw entries into one owned draft, lends that draft to pure execution-order analysis, returns sealed dense orders and an owned sorted list, and preserves deterministic DFS behavior.                                                                                                 |
| TLA pass                  | `crates/rolldown/src/stages/link_stage/passes/compute_tla.rs`                                                                                                                                | Reads only `ModuleTable`, consumes `TlaScanFacts`, emits require-TLA diagnostics through `PassCtx`, and returns sealed `TlaFacts` without changing the existing DFS behavior.                                                                                                                 |
| Representation passes     | `determine_module_formats.rs`, `compute_cjs_namespace_merges.rs`, `compute_dynamic_exports.rs`, `plan_module_wrapping.rs`, `create_wrapper_declarations.rs`, and `normalize_lazy_exports.rs` | Preserve serial first-observer format semantics, split downstream domains, seal dynamic-export facts, consume wrapper seeds exactly once, allocate wrapper identities only when declaration state begins, and finalize formats and wrappers after lazy normalization's identity invalidation. |
| Import binding            | `bind_imports.rs`                                                                                                                                                                            | Serially matches imports and immediately commits diagnostics, dependencies, namespace aliases, and symbol links while borrowing the retained typed facts; returns one short-lived owned envelope that the driver immediately destructures.                                                    |
| Resolved exports          | `collect_resolved_exports.rs` and `finalize_resolved_exports.rs`                                                                                                                             | Collects raw export-star maps in parallel, keeps the draft through binding, then classifies canonical ESM ambiguity after the last Link-stage symbol link and returns paired raw and sorted maps for a delayed no-clone projection.                                                           |
| CJS routing               | `compute_cjs_routing.rs`                                                                                                                                                                     | Computes only importers with named or namespace CJS routes, then drops named routes at member resolution and retains the sparse namespace subset needed by Generate.                                                                                                                          |
| Member resolution         | `resolve_member_expressions.rs`                                                                                                                                                              | Completes the graph-wide JSON escape union, preserves the existing indexed parallel analysis and warning reservation order, commits constant invalidation and dependency extensions serially, and returns checked normal-only resolution slots.                                               |
| Entry export roots        | `collect_entry_export_roots.rs`                                                                                                                                                              | Produces only sparse ordered roots for actual entries, preserving wrapper-first, preserve-signature, and dynamic-import usage semantics without writing metadata.                                                                                                                             |
| Synthetic statements      | `create_synthetic_export_statements.rs`                                                                                                                                                      | Owns and returns `IndexStmtInfos`, appends missing-export declarations, and replaces ESM namespace statements after M has observed the pre-synthetic graph.                                                                                                                                   |
| Reference analysis        | `reference_needed_symbols.rs`                                                                                                                                                                | Owns symbols and statements, performs the physical per-module analysis in parallel, seals statement runtime requirements, and returns only ordered `CallRuntimeRequire` patches for deferred module mutation.                                                                                 |
| Cross-module optimization | `passes/cross_module_optimization.rs` and `cross_module_optimization_runner.rs`                                                                                                              | Owns statements and draft constants, borrows only the final graph facts it needs, analyzes modules in parallel against an immutable per-round constant snapshot, commits in sorted order, seals unreachable dynamic imports, and finalizes constants.                                         |
| Shared inclusion core     | `tree_shaking/inclusion_core.rs`, `include_statements.rs`, `on_demand.rs`, and `passes.rs`                                                                                                   | Keeps one inclusion algorithm over narrow fact queries and invocation-local borrowed worksets. The legacy `IncludeContext` adapter delegates without allocation or a Generate call-site change; pass inventory rejects the core context and fact traits as pass carriers.                     |
| Tree-shaking pass         | `passes/tree_shake.rs` and `tree_shake_runner.rs`                                                                                                                                            | Consumes the entry plan, adapts typed artifacts to the shared inclusion queries for one invocation, preserves the two-phase scratch boundary, and returns retained entries, inclusion results, runtime requirements, used-symbol continuations, and deferred module patches.                  |
| Dependency finalization   | `passes/finalize_module_dependencies.rs` and `finalize_module_dependencies_runner.rs`                                                                                                        | Consumes dependency and runtime-helper drafts, performs immutable physical-slot analysis in parallel, commits serially in ascending module order, and returns final dependencies, load dependencies, helper masks, and side-effectful-runtime flags.                                          |
| Legacy output adapter     | `legacy_output_adapter.rs`                                                                                                                                                                   | Destructures final artifacts, validates all dense layouts before writes, applies deferred patches in legacy order, assembles module fields and metadata in one physical traversal, projects sparse facts, and constructs the unchanged Link output tuple.                                     |

## Public API and control flow

The implemented shape is:

```text
driver-owned PassPipelineCtx
  └── run_pass / run_infallible_pass
        ├── assert concrete pass size is zero during code generation
        ├── enter the pass tracing span
        ├── create RunBrand and RunToken<'_, P>
        ├── derive temporary write-only PassCtx
        └── P::run(token, cx, read, owned)
              └── token.finish(read_output, owned_output)
                    └── private-field RawPassOutput
        └── harness opens RawPassOutput
              ├── Sealed<OutputRead>
              └── OutputOwned
```

`Pass` has `InputRead<'a>`, `InputOwned`, `OutputRead`, `OutputOwned`, and `Error` associated types. `InputRead<'a>` is `Copy`; all other associated types are `'static`. Its `run` method receives `RunToken<'_, Self>`, where the anonymous token lifetime is bound to that invocation. `PassOutput<P>` names `(Sealed<P::OutputRead>, P::OutputOwned)`, and `PassResult<P>` names the corresponding result with `P::Error`. `run_pass` returns `PassResult<P>` without erasing the concrete associated error. `run_infallible_pass` is restricted to `Error = Infallible`, returns `PassOutput<P>`, and removes the uninhabited error by exhaustive matching.

`RunToken<'run, P>` contains a private mutable borrow of a harness-local `RunBrand`, an invariant lifetime marker, and a pass-type marker. It is neither `Copy` nor `Clone`. Its only public operation, `finish`, consumes it and creates `RawPassOutput`; that envelope's fields are private, so only the harness can open it.

`Sealed<T>` has a private field and only implements shared `Deref`. It has no public constructor, mutable dereference, or consuming unwrap. The harness constructs it unconditionally around `OutputRead`, including `OutputRead = ()`.

## Context ownership and diagnostics

The driver owns `PassPipelineCtx`. A serial pipeline uses one context; concurrent branches use separate contexts and append them to the parent in declared pass order. `append` preserves the order already present in each branch. `into_diagnostics` consumes the context and returns the existing `Diagnostics` type.

Only the harness constructs `PassCtx<'_>`, and each value exists for one invocation. The pass can call `push` or `extend`; it cannot construct, inspect, drain, or retain the underlying pipeline context. The harness stores the emitting pass's concrete type name with each diagnostic. When the pipeline context is consumed, that provenance is emitted to the `rolldown::pass` tracing target before the ordinary diagnostic is returned.

The context is a sink, not a dependency container. Pipeline data must remain in the declared read and owned slots.

## Compile and runtime coverage

`pass_harness.rs` contains three runtime cases:

- An infallible pass returns a sealed read-side fact while its owned output continues into another pass.
- A fallible pass returns its own typed error unchanged.
- Two branch contexts appended in declared order yield diagnostics in that same order. A local recording subscriber also pins the `run_pass` span, `rolldown::pass` event target, and concrete pass provenance for each merged diagnostic.

`pass_harness_ui.rs` first compiles `pass_valid.rs`, then compiles every `fail_*.rs` fixture expecting failure. The passing case is required because trybuild otherwise may use a check-only path; the non-zero-sized pass assertion lives in an inline const inside `run_pass` and must be exercised by code generation.

The fixtures pin these boundaries:

- External code cannot construct `RunToken`, `RawPassOutput`, or `Sealed`, inspect the raw or sealed private fields, unwrap a sealed value, obtain mutable access through it, or read `PassCtx`.
- A caller cannot invoke `Pass::run` without a token, use a token for the wrong pass, or leak the branded token through the associated error, either output, a panic payload, or a spawned thread.
- `InputRead` cannot contain a direct or nested `&mut`, and one owned input cannot be moved into sibling branches.
- Wrong driver order and a literal `()` pattern for `Sealed<()>` fail.
- A non-zero-sized pass fails in the code-generating test.

These tests do not prove the absence of interior mutability, globals, broad artifacts, inappropriate `Clone` access, duplicate values placed in a different output channel, or semantic ordering dependencies. They also do not prevent a pass from naming `PassPipelineCtx` in one of its own slots. Those remain source-test and review responsibilities described in `design.md`.

## Link adoption boundary

The selected adoption is the internals created by `LinkStage::new` and driven by `LinkStage::link`. Its external interface remains unchanged: input is `NormalizedScanStageOutput` plus `SharedOptions`, and `.link()` remains infallible and returns `(LinkStageOutput, IndexEcmaAst, UsedSymbolRefsBuilder)`. Code outside link does not need to understand the pass migration.

`LinkStage` is a two-field one-shot facade that holds only the untouched scan output and options until `.link()` consumes it. The first statement of `.link()` destructures the facade completely, and the next statement destructures the scan output into ordinary driver locals. The facade therefore does not survive into the first pass. No pass may accept `LinkStage`, `LinkStageOutput`, `LinkingMetadata`, `LinkingMetadataVec`, or a renamed equivalent.

The production driver has three parts:

```text
NormalizedScanStageOutput + SharedOptions
  └── input adapter: consume the two-field facade and move every Scan field into a driver local
        └── typed link driver: narrow artifacts and owned entity working sets
              └── legacy output adapter: consume final values once
                    └── existing mutable link output tuple
```

Neither adapter is a pass. The input adapter performs no traversal, sorting, aggregation, or link analysis; it only converts warnings into diagnostics and groups the two Scan-owned TLA inputs for their pass. Passthrough fields remain ordinary driver locals rather than entering the pass DAG. The output adapter is the only place that reconstructs `LinkStageOutput` and legacy `LinkingMetadataVec`, and its `finish` boundary lists every remaining Scan-owned final field explicitly instead of accepting the facade or another input bag.

### Legacy output ownership policy

The output adapter must construct the mutable legacy boundary without weakening `Sealed<T>`:

| Value kind                              | Pass channel  | Driver behavior                                                                                           | Final action                                                                                       |
| --------------------------------------- | ------------- | --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| Link-local fact read by later passes    | `OutputRead`  | Keep `Sealed<T>` and lend `&T` through dereference.                                                       | Drop after its last link consumer, or project a compact fact into one legacy field at the adapter. |
| Domain-final value required by Generate | `OutputOwned` | Keep ownership in a domain-specific final type, lend `&T` to readers, and do not expose general mutation. | Move it once into the legacy output adapter.                                                       |
| Mutable entity table or draft           | `OutputOwned` | Move it only into passes that mutate or consume it; unrelated passes receive narrow shared reads.         | Move it once into the legacy output tuple or drop it at its declared last use.                     |

There is no `Sealed::into_inner`, no adapter-only general unwrap, and no clone of a large table or map merely to cross the boundary. A value that must be moved into the legacy output stays on the owned channel. Its final domain type may restrict mutators with module privacy, but it is not described as mutation-sealed because Generate eventually receives a mutable legacy representation.

If a compact link-local sealed fact also has a legacy field, the adapter copies or projects that narrow fact and then drops the sealed artifact. No compatibility projection occurs before the adapter, so there is never a second persistent representation or a way to pass legacy metadata back into the DAG.

## Current link field lifecycles

These tables record the post-cutover flow. “Last consumer” means the last semantic Link reader before the output adapter. The former blanket metadata dump is gone; the adapter emits only an artifact-scoped module-count trace, so it does not extend every metadata field's lifetime.

### Driver and boundary values

| Value                                                                                                       | Producer or initialization                            | Last Link consumer                                      | Boundary action                                                      |
| ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------------- |
| `module_table`                                                                                              | Scan; selected passes own or mutate it                | H and G borrow it; the adapter applies deferred patches | Move into `LinkStageOutput`                                          |
| `EntryPlanDraft`                                                                                            | `CanonicalizeEntriesPass` consumes raw scan entries   | H consumes it into `RetainedEntries`                    | Adapter moves retained entries into `entries`                        |
| `SymbolRefDb`                                                                                               | Scan, then wrapper/lazy/binding/reference writers     | G                                                       | Move outward; return H's `UsedSymbolRefsBuilder` separately          |
| `IndexStmtInfos`                                                                                            | Scan, then wrapper/lazy/synthetic/reference/P writers | G                                                       | Move outward                                                         |
| `RuntimeModuleBrief`                                                                                        | Scan                                                  | H; G receives only the copied runtime `ModuleIdx`       | Move the brief outward unchanged                                     |
| `SortedModules`                                                                                             | `ComputeModuleExecutionOrderPass`                     | P                                                       | Adapter moves its vector outward                                     |
| `LinkingMetadataVec`                                                                                        | Not present in `LinkStage`                            | No pass reads it                                        | Adapter allocates defaults exactly once and projects all final facts |
| `PassPipelineCtx`                                                                                           | Driver                                                | G is the last pass                                      | Drain after G; move diagnostics outward                              |
| `IndexEcmaAst`                                                                                              | Scan; lazy normalization may mutate it                | P                                                       | Return as the tuple's second element                                 |
| `StatementRuntimeRequirements`                                                                              | N                                                     | H                                                       | Drop immediately after H                                             |
| `UnreachableDynamicImports`                                                                                 | P                                                     | H                                                       | Drop immediately after H                                             |
| dependency and runtime-helper drafts                                                                        | C/M and H                                             | G consumes both                                         | Adapter receives only `FinalizedModuleDependencies`                  |
| `InclusionResults` and `RetainedEntries`                                                                    | H                                                     | G                                                       | Adapter projects inclusion and moves entries                         |
| used-symbol continuations and enum presence                                                                 | H                                                     | None                                                    | Adapter moves or projects them                                       |
| formats, wrappers, resolved exports, CJS routing, included CJS symbols, constants, and normal export chains | Earlier typed passes                                  | H                                                       | Retain unchanged until adapter projection                            |
| side effects, member resolutions, and entry roots                                                           | Earlier typed passes                                  | G                                                       | Retain unchanged until adapter projection                            |
| execution order, TLA, dynamic exports, shims, external stars, merge maps, and deferred patches              | Earlier typed passes                                  | None after their named typed readers                    | Retain until adapter projection or application                       |
| scan passthrough maps                                                                                       | Scan                                                  | Named readers only, if any                              | Move directly through the adapter                                    |

`retained_export_symbols` remains a Generate-owned default initialized by the output adapter. It is neither a Link product nor a scan passthrough.

### `LinkingMetadata`

`LinkingMetadataVec` exists only inside `LegacyOutputAdapter::finish`. The adapter consumes dense owned artifacts into their final slots, validates every count and normal/external slot shape before any output mutation, preserves the deferred JSON-replacement, `DeadDynamicImport`, and `CallRuntimeRequire` patch order, and then performs one physical-module traversal. That traversal mutates the final module fields and default-constructs, fills, and pushes one metadata slot at a time; compact TLA and dynamic-export bitsets, CJS routes, and entry roots remain sparse projections. No graph-sized artifact is cloned.

| Typed source                                              | Metadata destination                                                               | Last typed Link reader before projection                     |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| `ModuleWrappers`                                          | wrapper kind/ref/statement and `required_by_other_module`                          | H                                                            |
| `ResolvedExports`                                         | raw and sorted canonical export maps                                               | H                                                            |
| `DynamicExports`, `TlaFacts`, and `ShimmedMissingExports` | their existing compact fields                                                      | N / adapter only / synthetic statement creation respectively |
| `CjsRoutingFinal` and `IncludedCommonJsExportSymbols`     | CJS namespace routes and included CJS symbols                                      | H                                                            |
| `MemberExprResolutions` and `EntryExportRoots`            | member resolutions and entry-root references                                       | G                                                            |
| `ExternalStarExports`                                     | external-star import-record ids                                                    | synthetic statement creation                                 |
| `InclusionResults`                                        | statement/module inclusion bits and phase-one namespace reasons                    | G                                                            |
| `FinalizedModuleDependencies`                             | dependencies, load dependencies, runtime helpers, and side-effectful-runtime flags | Adapter only                                                 |

Generate-owned fields such as `namespace_included`, `init_is_noop`, `transitive_esm_init_targets`, and concatenation state remain default. `wrapper_stmt_info` remains an index into the final `IndexStmtInfos[module_idx]`; a pristine JSON rebuild clears or recreates wrapper identity before `ModuleWrappers` becomes final, and the adapter never replaces that index with a boolean.

### Embedded facts and deferred module patches

| Embedded field or mutation                                                             | Producer or last writer                                                                                                                                                                                                                                                                  | Last link consumer                                                                                                                                                      | Current treatment                                                                                                                                                                                                                 |
| -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Module::exec_order`                                                                   | Sealed `ModuleExecutionOrders` from `ComputeModuleExecutionOrderPass`                                                                                                                                                                                                                    | `BindImportsPass` reads the typed artifact                                                                                                                              | Retain through binding; adapter projects assigned non-sentinel slots once                                                                                                                                                         |
| `NormalModule::exports_kind`                                                           | Scan draft; format classification; lazy JSON is the last writer                                                                                                                                                                                                                          | H reads final `ModuleFormats`                                                                                                                                           | Adapter projects final normal slots once                                                                                                                                                                                          |
| `wrap_kind`, `wrapper_ref`, `wrapper_stmt_info`                                        | Format seeds, wrapping plan and declaration allocation; lazy JSON clears all three when it rebuilds the module's statements                                                                                                                                                              | H reads final `ModuleWrappers`                                                                                                                                          | Adapter projects the paired declaration state once                                                                                                                                                                                |
| `Module::side_effects`                                                                 | Scan draft; sealed `ModuleSideEffects` finalizes it                                                                                                                                                                                                                                      | G                                                                                                                                                                       | Adapter projects normal slots once after G                                                                                                                                                                                        |
| `NormalModule::constant_export_map`                                                    | Scan; consumed by `ExtractGlobalConstantsPass` when inline constants are enabled                                                                                                                                                                                                         | Constant extraction itself                                                                                                                                              | Move into `GlobalConstantsDraft`; disabled mode preserves the per-module maps, matching existing behavior                                                                                                                         |
| `ImportRecord::meta::CallRuntimeRequire`                                               | Reference analysis                                                                                                                                                                                                                                                                       | No later link reader                                                                                                                                                    | Emit `ReferenceImportRecordPatches` and apply once at the adapter                                                                                                                                                                 |
| `ImportRecord::meta::DeadDynamicImport`                                                | Inclusion                                                                                                                                                                                                                                                                                | No later link reader                                                                                                                                                    | Emit `TreeShakeModulePatches` and apply once at the adapter                                                                                                                                                                       |
| `EcmaView::json_module_none_self_reference_included_symbol`                            | Inclusion                                                                                                                                                                                                                                                                                | No later link reader                                                                                                                                                    | Emit `TreeShakeModulePatches` and apply once at the adapter                                                                                                                                                                       |
| Lazy JSON `named_exports`, `ast_usage`, AST, local symbol DB, and statement rebuilding | Lazy normalization                                                                                                                                                                                                                                                                       | `CollectResolvedExportsPass` first reads the final named-export identities; binding, reference analysis, or inclusion remains the later consumer depending on the field | Keep together in the explicit one-call owned lazy-normalization manifest; only a pristine object rebuild replaces owner-local identities, while every fallback preserves existing `SymbolRef`, `NodeId`, and `StmtInfoIdx` values |
| `StmtInfoMeta::LazyExportPayload`                                                      | Parse records the loader statement's arena address without changing the hook-visible AST, accepts an identity-losing replacement only when one expression candidate remains, and passes the resolved body index to Scan; Scan fails the build if the statement table cannot represent it | `NormalizeLazyExportsPass`                                                                                                                                              | Require exactly one marked expression in link; remove the bit when the fallback wraps the payload, while a pristine rebuild replaces the marked table so no marker survives link                                                  |
| `NonSplittableJsonDefaults`                                                            | `NormalizeLazyExportsPass` detects accessor-shaped JSON fallbacks                                                                                                                                                                                                                        | `ResolveMemberExpressionsPass`                                                                                                                                          | Move into M, canonicalize and union with graph-wide mutation/escape facts, then omit it from the output                                                                                                                           |
| `LazyJsonExportInitializers`                                                           | `NormalizeLazyExportsPass` appends ordinary binding and synthetic statement identities                                                                                                                                                                                                   | Generate module finalization                                                                                                                                            | Cross the unchanged boundary sparsely, `mem::take` immediately before parallel finalization, materialize retained bindings after each payload, then drop                                                                          |
| `StmtInfo::{declared_symbols, referenced_symbols, eval_flags, import_records, meta}`   | Scan plus wrapping and lazy writers; M is the last pre-synthetic reader; `CreateSyntheticExportStatementsPass`, `ReferenceNeededSymbolsPass`, and `CrossModuleOptimizationPass` write afterward                                                                                          | Inclusion and dependency finalization                                                                                                                                   | Keep in owned `IndexStmtInfos`; pass ownership through all three tail writers and do not repackage it as a broad read artifact                                                                                                    |

The adapter applies deferred module mutations in the legacy order: replace each JSON non-self-reference set, insert `DeadDynamicImport` without clearing other bits, then apply N's `CallRuntimeRequire` patches. Focused tests pin replacement rather than union semantics and coexistence of both import-record flags.

## Pass module conventions for link

The link passes subtree declares `#![forbid(unsafe_code)]`. Each pass token is a non-generic unit struct named `XxxPass`; runtime configuration belongs in its slot manifest. The harness's code-generation size assertion remains a backstop, not a source-shape checker.

The non-vacuous `inventory` test parses every Rust file below the subtree, including local items. It pairs every pass declaration with exactly one unqualified `Pass` implementation; rejects generic, tuple, braced, qualified, renamed, local-hidden, macro-generated, and unmatched shapes; bans glob imports that could hide the trait; and rejects `LinkStage`, `LinkStageOutput`, `LinkingMetadata`, `LinkingMetadataVec`, `PassPipelineCtx`, `InclusionCoreContext`, `InclusionFacts`, and `InclusionModuleFacts` paths in production pass code. Separate executable guards require `LinkStage` to have exactly the `scan_stage_output` and `options` fields, require `.link()` to consume both the facade and every Scan field before any driver work, reject later `self` use or extra `LinkStage` methods, and pin the output adapter's explicit final-field and helper signatures. They also reject any exact `LinkStage` path or additional replacement struct in the adapter module. Production attributes use a closed allowlist of documentation and the built-in `Clone`, `Copy`, `Debug`, and `Default` derives. Conditional imports are limited to the exact private WASM `IteratorExt as _` shim and the exact private native `IndexedParallelIterator` traits needed to preserve M's and N's legacy zipped indexed iterators; the inventory checks target expression, full path, rename, visibility, and leading-colon shape, and rejection fixtures pin nearby variants. Production macro calls use a closed list of exact standard or dependency paths, including path-scoped layout and hard-invariant assertions for the extracted passes; type macros and nested macro calls are rejected, and every allowed macro argument is recursively scanned for forbidden carrier names. Focused rejection tests pin these cases. This remains a source-test boundary, so review must extend it if Rust syntax or the pass conventions change.

## Current production chain

`LinkStage::new` stores `NormalizedScanStageOutput` and the options reference without projecting either one. `LinkStage::link` immediately consumes those two facade fields, destructures every Scan field into a local, converts warnings into diagnostics, groups the two scan-only TLA inputs, creates one `PassPipelineCtx`, and runs this statically visible serial chain:

```text
ExtractGlobalConstantsPass
  → CanonicalizeEntriesPass
  → CollectInitialDependenciesPass
  → CollectExternalStarExportsPass
  → ComputeModuleExecutionOrderPass
  → ComputeTlaPass
  → DetermineModuleFormatsPass
  → ComputeCjsNamespaceMergesPass
  → ComputeDynamicExportsPass
  → PlanModuleWrappingPass
  → CreateWrapperDeclarationsPass
  → NormalizeLazyExportsPass
  → DetermineModuleSideEffectsPass
  → CollectResolvedExportsPass
  → BindImportsPass
  → FinalizeResolvedExportsPass
  → ComputeCjsRoutingPass
  → ResolveMemberExpressionsPass
  → CollectEntryExportRootsPass
  → CreateSyntheticExportStatementsPass
  → ReferenceNeededSymbolsPass
  → CrossModuleOptimizationPass
  → TreeShakePass
  → FinalizeModuleDependenciesPass
```

| Pass                                  | Read slot                                                                                                                                                                                                                                                    | Owned slot                                                                                                                                               | Read-side result                      | Owned result and last use                                                                                                                                                                                                                                                                                                   |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ExtractGlobalConstantsPass`          | `ConstantExtractionInput { enabled }`                                                                                                                                                                                                                        | `ModuleTable`                                                                                                                                            | `()`                                  | Returns the same table plus `GlobalConstantsDraft`. Lazy normalization borrows the draft; M owns and invalidates it; P finalizes `GlobalConstants`; H reads the final map; the adapter consumes it.                                                                                                                         |
| `CanonicalizeEntriesPass`             | `&ModuleTable`                                                                                                                                                                                                                                               | `Vec<EntryPoint>`                                                                                                                                        | `()`                                  | Uniquely produces `EntryPlanDraft`. It preserves user entry order, canonicalizes the discovered suffix, exposes identity owners to lazy normalization and P, feeds entry-root collection, and is finally consumed by H.                                                                                                     |
| `CollectInitialDependenciesPass`      | `&ModuleTable`                                                                                                                                                                                                                                               | `()`                                                                                                                                                     | `()`                                  | Produces `ModuleDependenciesDraft`; binding and M extend it before G consumes it. It excludes dynamic-import, require, and HMR-only `HotAccept` records.                                                                                                                                                                    |
| `CollectExternalStarExportsPass`      | `&ModuleTable`                                                                                                                                                                                                                                               | `()`                                                                                                                                                     | `()`                                  | Produces `ExternalStarExports`. Its typed lifetime crosses synthetic statement creation and ends only when the compatibility adapter moves the unchanged record indices onward for Generate.                                                                                                                                |
| `ComputeModuleExecutionOrderPass`     | `ComputeModuleExecutionOrderInput<'_>` borrowing `ModuleTable` and the unique `EntryPlanDraft`                                                                                                                                                               | `()`                                                                                                                                                     | Sealed `ModuleExecutionOrders`        | Returns owned `SortedModules`. Binding reads the sealed orders for deterministic external facade names, P reads the sorted list, and the adapter performs the only compatibility projection.                                                                                                                                |
| `ComputeTlaPass`                      | `&ModuleTable`                                                                                                                                                                                                                                               | `TlaScanFacts`                                                                                                                                           | Sealed `TlaFacts`                     | `()`; the compact fact remains sealed until the adapter projects only set bits.                                                                                                                                                                                                                                             |
| `DetermineModuleFormatsPass`          | `DetermineModuleFormatsInput<'_>` borrowing `ModuleTable` and `EntryPlanDraft`                                                                                                                                                                               | `()`                                                                                                                                                     | `()`                                  | Returns owned `ModuleFormatsDraft` and `WrapperSeeds`. The pass preserves physical module and import-record order and does not mutate its inputs.                                                                                                                                                                           |
| `ComputeCjsNamespaceMergesPass`       | `ComputeCjsNamespaceMergesInput<'_>` borrowing `ModuleTable` and `ModuleFormatsDraft`                                                                                                                                                                        | `()`                                                                                                                                                     | `()`                                  | Returns owned `CjsNamespaceMerges`, preserving namespace-reference order and strict-execution-order disablement; the typed map survives through lazy-normalization and N before the adapter converts it once.                                                                                                               |
| `ComputeDynamicExportsPass`           | `ComputeDynamicExportsInput<'_>` borrowing `ModuleTable` and `ModuleFormatsDraft`                                                                                                                                                                            | `()`                                                                                                                                                     | Sealed `DynamicExports`               | `()`; preserves the existing export-star DFS, cycle cache behavior, and dense module identity.                                                                                                                                                                                                                              |
| `PlanModuleWrappingPass`              | `PlanModuleWrappingInput<'_>` borrowing `ModuleTable` and `ModuleFormatsDraft`                                                                                                                                                                               | `WrapperSeeds`                                                                                                                                           | `()`                                  | Returns owned `WrapperPlan`; uniquely consumes seeds, preserves recursive propagation and strict/on-demand overrides, and carries `required_by_other_module` for every module slot.                                                                                                                                         |
| `CreateWrapperDeclarationsPass`       | `CreateWrapperDeclarationsInput<'_>` borrowing `ModuleTable` and two driver-selected helper refs                                                                                                                                                             | `WrapperPlan`, `SymbolRefDb`, and `IndexStmtInfos`                                                                                                       | `()`                                  | Returns the same owned entity tables plus `WrapperDeclarationsDraft`; declaration variants atomically pair kind, symbol, and statement identity, while each slot independently preserves `required_by_other_module`.                                                                                                        |
| `NormalizeLazyExportsPass`            | `NormalizeLazyExportsInput<'_>` borrowing `EntryPlanDraft`, `CjsNamespaceMerges`, and `GlobalConstantsDraft`                                                                                                                                                 | One short-lived manifest containing `ModuleTable`, `IndexEcmaAst`, `IndexStmtInfos`, `SymbolRefDb`, `ModuleFormatsDraft`, and `WrapperDeclarationsDraft` | `()`                                  | Returns the same four large entity allocations, final `ModuleFormats` and `ModuleWrappers`, Generate-only `LazyJsonExportInitializers`, and member-resolution-only `NonSplittableJsonDefaults`. It finalizes only after preserving or deliberately rebuilding local identities.                                             |
| `DetermineModuleSideEffectsPass`      | `DetermineModuleSideEffectsInput<'_>` borrowing `ModuleTable`, sealed `DynamicExports`, and final `ModuleWrappers`                                                                                                                                           | `()`                                                                                                                                                     | Sealed `ModuleSideEffects`            | Preserves the legacy ordered DFS and three-state cache. Binding, M, N, H, and G read the sealed artifact; only the adapter projects normal-module slots.                                                                                                                                                                    |
| `CollectResolvedExportsPass`          | `&ModuleTable` after lazy normalization                                                                                                                                                                                                                      | `()`                                                                                                                                                     | `()`                                  | Returns owned `ResolvedExportsDraft`, with `None` for external and `Some(map)` for normal slots. Independent physical roots run in parallel; each path-local DFS stays serial. The draft remains typed through binding and then moves into finalization.                                                                    |
| `BindImportsPass`                     | `BindImportsInput<'_>` borrowing `ModuleTable`, `ResolvedExportsDraft`, final `ModuleFormats`, sealed `DynamicExports`, sealed `ModuleSideEffects`, and sealed `ModuleExecutionOrders`; copies only output format and the shim flag                          | `BindImportsOwned` containing `SymbolRefDb` and `ModuleDependenciesDraft`                                                                                | `()`                                  | Serially preserves recursive matching and immediate diagnostic, dependency, namespace-alias, and symbol-link commits. It privately groups external ESM bindings and commits facade links at the end. Its one-call output envelope returns symbols plus five purpose-specific owned results and is immediately destructured. |
| `FinalizeResolvedExportsPass`         | `&SymbolRefDb` after binding's final Link-stage links                                                                                                                                                                                                        | `ResolvedExportsDraft`                                                                                                                                   | `()`                                  | Returns final owned `ResolvedExports`, pairing each unchanged raw map with a sorted canonical non-ambiguous map. It runs per module in parallel, remains typed through H, and moves into the adapter without cloning.                                                                                                       |
| `ComputeCjsRoutingPass`               | `ComputeCjsRoutingInput<'_>` borrowing `ModuleTable`, final `ModuleFormats`, and sealed `DynamicExports`                                                                                                                                                     | `()`                                                                                                                                                     | `()`                                  | Returns sparse `CjsRoutingDraft` entries only for importers with named or namespace routes. Named routes include direct and dynamic CJS relations; namespace routes include direct CommonJS only.                                                                                                                           |
| `ResolveMemberExpressionsPass`        | `ResolveMemberExpressionsInput<'_>` borrowing `ModuleTable`, `IndexStmtInfos`, `SymbolRefDb`, final `ResolvedExports`, `NormalExportChains`, sealed `ModuleSideEffects`, and sealed `DynamicExports`                                                         | `CjsRoutingDraft`, `NonSplittableJsonDefaults`, `GlobalConstantsDraft`, and `ModuleDependenciesDraft`                                                    | `()`                                  | Completes the JSON union before indexed per-module analysis, appends warnings in the preserved `AppendOnlyVec` reservation order, serially invalidates constants and extends dependencies, returns normal-only resolution slots, and converts routing to namespace-only `CjsRoutingFinal`.                                  |
| `CollectEntryExportRootsPass`         | `CollectEntryExportRootsInput<'_>` borrowing `ModuleTable`, `EntryPlanDraft`, final `ModuleWrappers`, final `ResolvedExports`, dynamic-import usage, and preserve-signature settings                                                                         | `()`                                                                                                                                                     | `()`                                  | Returns sparse ordered `EntryExportRoots`. It validates every dense layout before collection, keeps only nonempty actual entry roots, places a wrapper first, preserves canonical export order, and enforces the pre-Link `Single`-usage merge invariant.                                                                   |
| `CreateSyntheticExportStatementsPass` | `CreateSyntheticExportStatementsInput<'_>` borrowing `ModuleTable`, final `ModuleFormats`, final `ResolvedExports`, `ShimmedMissingExports`, `ExternalStarExports`, helper refs, and output options                                                          | `IndexStmtInfos`                                                                                                                                         | `()`                                  | Returns the owned statement table after appending shim declarations and replacing ESM namespace statements. It validates all dense and normal/external slot shapes before mutation and runs after M's deliberate pre-synthetic graph read.                                                                                  |
| `ReferenceNeededSymbolsPass`          | `ReferenceNeededSymbolsInput<'_>` borrowing `ModuleTable`, final formats and wrappers, sealed dynamic exports and side effects, `CjsNamespaceMerges`, one optional runtime ref, and scalar options                                                           | `ReferenceNeededSymbolsOwned` containing `SymbolRefDb` and `IndexStmtInfos`                                                                              | Sealed `StatementRuntimeRequirements` | Returns the two owned entity tables plus ordered `ReferenceImportRecordPatches`. Its four-way indexed per-module traversal is parallel and disjoint; only `CallRuntimeRequire` metadata is deferred, and inclusion borrows the sealed helper table.                                                                         |
| `CrossModuleOptimizationPass`         | `CrossModuleOptimizationInput<'_>` borrowing `ModuleTable`, `IndexEcmaAst`, `SymbolRefDb`, `SortedModules`, `EntryPlanDraft`, and `MemberExprResolutions`; copies `FlatOptions` and borrows shared options                                                   | `CrossModuleOptimizationOwned` containing `IndexStmtInfos` and `GlobalConstantsDraft`                                                                    | Sealed `UnreachableDynamicImports`    | Runs bounded rounds with parallel per-module analysis against an immutable constant snapshot and serial sorted commit; returns the statement table plus final `GlobalConstants`. H is the only consumer of the sealed unreachable-import fact.                                                                              |
| `TreeShakePass`                       | `TreeShakeInput<'_>` borrowing module, statement, symbol, runtime, format, wrapper, export, routing, dependency, resolution, side-effect, constant, root, chain, CJS-symbol, dynamic-usage, runtime-requirement, unreachable-import, and scalar option facts | `TreeShakeOwned` containing `EntryPlanDraft`                                                                                                             | `()`                                  | Returns `RetainedEntries`, `InclusionResults`, `ModuleRuntimeRequirementsDraft`, used-symbol continuations, `TreeShakeModulePatches`, and enum presence. Its first phase owns observable scratch; its fresh runtime phase shares only inclusion and used-symbol sets and discards all other scratch.                        |
| `FinalizeModuleDependenciesPass`      | `FinalizeModuleDependenciesInput<'_>` borrowing modules, statements, symbols, H inclusion, member resolutions, side effects, retained entries, entry roots, runtime id, and the tree-shaking flag                                                            | `FinalizeModuleDependenciesOwned` containing `ModuleDependenciesDraft` and `ModuleRuntimeRequirementsDraft`                                              | `()`                                  | Returns `FinalizedModuleDependencies`. Indexed analysis reads immutable pre-commit snapshots in parallel; serial ascending physical commit preserves dependency/load ordering and prevents same-pass transitive helper inheritance.                                                                                         |

This chain retains each artifact through its actual final typed consumer. `EntryPlanDraft` is borrowed by ordering, format classification, lazy rebuild eligibility, entry-root collection, and P before H consumes it. Final formats, wrappers, resolved exports, CJS routing, included CommonJS symbols, constants, and normal export chains remain typed through H. Side effects, member resolutions, and entry roots remain typed through G. Execution order, TLA, dynamic exports, shims, external stars, merge maps, sorted modules, H results, and deferred patches remain outside legacy representations until the output adapter.

The driver retains six binding inputs across otherwise unrelated work: `ModuleDependenciesDraft`, sealed `ModuleExecutionOrders`, final `ModuleFormats`, sealed `DynamicExports`, sealed `ModuleSideEffects`, and `ResolvedExportsDraft`. `BindImportsPass` owns dependencies and the symbol database, borrows the other five facts, and returns one envelope that the driver immediately destructures. There is no early compatibility projection; subsequent passes receive the typed results directly.

Binding remains one serial commit path by deliberate production-cutover decision. Recursive matching emits diagnostics and commits dependency additions, namespace aliases, and `SymbolRefDb::link` calls before advancing to the next named import. External ESM binding groups are private pass state; after the main traversal, the pass sorts property entries within each group when needed and commits their facade links. Separating analysis would require simulating those earlier commits or retaining a second ordered effect transcript, so it is rejected here rather than recorded as unfinished migration work.

After binding returns, `FinalizeResolvedExportsPass` receives the linked symbol database and owned draft. The final artifact pairs raw maps with sorted canonical non-ambiguous maps. `ComputeCjsRoutingPass` reads final formats and dynamic exports and returns only sparse importer entries. `ResolveMemberExpressionsPass` then borrows final exports, normal export chains, side effects, and dynamic exports while owning routing, the JSON guard, constants, and dependencies. Named routing and the JSON guard die inside M. M must finish before synthetic export statements because its JSON mutation and escape scan deliberately reads the pre-synthetic statement graph.

The six-pass tail keeps each remaining fact only as long as needed. Roots feed H and G; synthetic statement creation, N, and P successively own the statement table. H consumes entries, runs the inclusion fixpoint, records phase-one namespace reasons and module patches, normalizes runtime requirements, then runs runtime-helper inclusion with fresh discard-only scratch. The driver drops N's statement requirements and P's unreachable-import facts immediately afterward. G consumes dependency and runtime drafts and returns their final form. Only then does the driver drain diagnostics and invoke the adapter. Generate later performs separate code-splitting symbol links against the projected output, outside the Link pass chain.

### Import-binding contract

| Slot            | Type                | Contents and lifecycle                                                                                                                                                                                                                                                                                                                                                                              |
| --------------- | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `InputRead<'a>` | `BindImportsInput`  | Shared `ModuleTable`, `ResolvedExportsDraft`, final `ModuleFormats`, sealed `DynamicExports`, sealed `ModuleSideEffects`, and sealed `ModuleExecutionOrders`, plus copied `OutputFormat` and `shim_missing_exports`. These references are valid only for the call.                                                                                                                                  |
| `InputOwned`    | `BindImportsOwned`  | The unique `SymbolRefDb` and `ModuleDependenciesDraft`. Binding may create facade symbols, commit links, and extend dependencies without exposing mutation through a read slot.                                                                                                                                                                                                                     |
| `OutputRead`    | `()`                | Binding does not mint a shared fact.                                                                                                                                                                                                                                                                                                                                                                |
| `OutputOwned`   | `BindImportsOutput` | Updated `SymbolRefDb` and `ModuleDependenciesDraft`, plus `ShimmedMissingExports`, `IncludedCommonJsExportSymbols`, `NormalExportChains`, and `ExternalImportNamespaceMerges`. The driver immediately destructures the envelope; H reads included CommonJS symbols, normal export chains, and dependencies before G consumes the dependency draft, and the adapter consumes every remaining result. |
| `Error`         | `Infallible`        | The unchanged Link boundary remains infallible; diagnostics flow through `PassCtx`.                                                                                                                                                                                                                                                                                                                 |

`BindImportsOutput` is an ownership envelope for one return edge, not a reusable state object. No pass accepts it, and the driver never stores it. Its five non-symbol results remain purpose-specific: included CommonJS symbols and normal export chains remain typed through H; shims remain typed through synthetic statement creation; dependencies move through M, are borrowed by H, and are consumed by G; external namespace merges remain owned until the output adapter. None is projected early.

Focused binding tests pin direct imports and physical slot shapes, reexport-chain dependency order, dynamic and CommonJS fallbacks, one shared shim across importers, missing-then-ambiguous diagnostic content and order, and grouped external ESM facade naming. Dense input and compatibility-projection layouts are fail-closed internal invariants rather than recoverable partial outputs. The inventory separately pins the broad-carrier exclusions, the three production `SymbolRefDb::link` method calls, rejection of associated-path, imported-alias, raw-identifier, and macro-token bypasses, and the exact path-scoped tracing, formatting, assertion, and unreachable-invariant macros retained by this extraction.

Lazy normalization is an atomic mutation boundary because AST, semantic symbols, statement information, format state, and wrapper identity must change together. Its six-field owned manifest exists only for the call and is immediately destructured; it is not retained as a general link state. Object-form JSON uses the independently tree-shakeable binding rebuild only when the AST, owner-local side tables, statement table and reverse maps, facade-symbol database, and optional ESM wrapper exactly match the JSON loader's pristine state. Entry-related identities, CJS namespace merges, and extracted constants mark owners that cannot be rebuilt. Before `transformAst`, Parse records the loader-created statement's arena address in a side channel without changing the AST that hooks observe. The address survives moves and in-place edits. If a hook replaces the whole statement and loses that identity, Parse accepts the result only when exactly one expression-statement candidate remains; multiple candidates produce `TRANSFORM_ERROR` before Scan instead of any structural or positional guess. Scan converts the resolved body index into exactly one `StmtInfoMeta::LazyExportPayload` or fails the build if the statement table cannot represent it. Link accepts only that unique marker and never guesses from expression order. Any transformed or otherwise non-pristine module keeps its existing AST, `SymbolRef`, `NodeId`, and `StmtInfoIdx` identities. Its JSON keys receive ordinary appended facade bindings plus synthetic `LazyJsonExportInitializer` statements, never `NamespaceAlias`; binding can therefore tree-shake individual snapshot bindings and all formats use normal symbol handling. A sparse recipe artifact carries only retained initializer identities to Generate, where it is taken from `LinkStageOutput`, read in parallel, materialized immediately after the payload, and dropped before later Generate work. Accessor-shaped fallbacks separately produce `NonSplittableJsonDefaults`; `ResolveMemberExpressionsPass` canonicalizes that guard, unions it with graph-wide mutation and escape facts, and drops it after preventing live `default.key` reads from being rewritten to snapshots. A successful pristine rebuild replaces `StmtInfos` wholesale so its private reverse maps cannot retain stale identities, recreates namespace/default/HMR facades, carries the allowed external wrapper-helper reference, and clears the invalidated wrapper declaration before formats and wrappers become final.

The execution-order traversal preserves the old iterative DFS. Its cycle deduplication uses first-discovery `FxIndexSet` order instead of nondeterministic `FxHashSet` iteration, making independent cycle warnings stable across fresh processes. The driver shares one serial pipeline context across all twenty-four passes and drains it after G. Provenance and append order therefore remain explicit: execution-order cycle warnings precede require-TLA diagnostics, both precede import-binding diagnostics, and member-resolution warnings follow binding while retaining the existing indexed-parallel `AppendOnlyVec` worker-reservation order. The six passes after M are infallible and emit no diagnostics, so the later drain does not alter diagnostic order.

### TLA contract

`ComputeTlaPass` has the following concrete contract:

| Slot            | Type              | Lifecycle                                                                                          |
| --------------- | ----------------- | -------------------------------------------------------------------------------------------------- |
| `InputRead<'a>` | `&'a ModuleTable` | Shared for this invocation only; module identity owns every `ModuleIdx` in the result.             |
| `InputOwned`    | `TlaScanFacts`    | Moves the direct-TLA count and keyword-span map into their only link consumer.                     |
| `OutputRead`    | `TlaFacts`        | Sealed by the harness; stores the module count and an `IndexBitSet<ModuleIdx>` of tainted modules. |
| `OutputOwned`   | `()`              | No mutable domain value continues.                                                                 |
| `Error`         | `Infallible`      | Eliminated exhaustively by `run_infallible_pass`; the external link signature stays infallible.    |

The direct-TLA count preserves the old constant-time common path: when it is zero, `TlaFacts` contains a zero-capacity bitset and neither allocates nor scans metadata. Otherwise, the moved DFS is unchanged, including its documented cycle memoization limitation, direct-edge preference in diagnostic chains, module and import-record iteration order, and release fallback for a missing keyword span.

The adapter checks the artifact's module count and copies only set bits into its freshly default metadata. The generic consuming-crate `fail_mutate_sealed` fixture proves that `Sealed<TlaFacts>` cannot be mutated; the focused pass test additionally pins the real associated output type.

## Production-cutover decisions

The `Pass` trait is retained after use by all twenty-four production passes. Its branded execution authority, `InputRead: Copy`, owned mutation channel, unconditional sealing, typed infallibility, tracing, and diagnostic provenance justify the GAT and slot ceremony; helpers and per-module algorithms remain ordinary functions.

`Infallible` is an error-channel property of the harness and Link driver, not a transitive promise that every internal algorithm is a total function. `run_infallible_pass` exhaustively eliminates `Infallible`; `LinkStage::link` keeps its direct tuple return; and the unchanged `bundle_up` initializer adds no `Result`, `?`, `unwrap`, `expect`, or panic-based error adapter. A source-level AST guard pins those exact boundaries. Local `Option` control flow and fail-closed artifact invariants retain their predecessor semantics: malformed counts, slot shapes, owners, identities, or patch targets must not become partial output through a skip or default.

`BindImportsPass` remains serial for the ordering reasons above. This is an explicit rejection of an analysis/commit split in this cutover, not a pending implementation item. Likewise, no dense artifact is converted speculatively to `Box<[T]>` or `IndexBox`: current private `IndexVec` and bitset APIs already enforce dense identity, drafts expose only the mutation or owning iteration they need, and final facts expose read-only queries before the adapter either projects selected fields or moves an existing owned container. Without a new representation invariant, reboxing would add conversion risk without a lifecycle benefit.

Performance acceptance is final-structure-only. Intermediate structural commits are validated for correctness, diagnostics, determinism, builds, and code quality but are not timing or RSS candidates. `baseline.md` records the repository-owned method and historical method-validation run; the exact complete-candidate comparison and its immutable parent belong in the PR evidence ledger so a machine-specific result cannot silently describe a later tree. This document makes no numerical claim before that evidence is accepted.

## Current phase status

- Phase 0 machinery exists in `rolldown_utils`: the branded token, private raw envelope, typed error channel, infallible entry, contexts, sealing wrapper, runtime tests, and compile fixtures are present.
- Phase 1 baseline code exists behind non-default features: the timing boundary, same-run diagnostic observer, deterministic synthetic and pinned real workloads, versioned reports, trace attribution, digest model, and scan/link process-RSS controls are present. Canonical measured results and budgets are recorded separately in `baseline.md` once accepted.
- Phase 2's first production slice exists: `ComputeTlaPass`, `TlaScanFacts`, sealed compact `TlaFacts`, focused algorithm tests, production trace coverage, and the link source inventory are implemented. Its compatibility projection now occurs only in the final adapter.
- Phase 3's initialization, execution-order, representation, and side-effect tranches exist as thirteen narrow passes: `EntryPlanDraft` and `ModuleFormatsDraft` have real typed consumers; wrapper seeds and plans each have one consuming edge; wrapper declaration identities are allocated in a representation-safe draft; lazy normalization performs the final draft-to-final transition for formats and wrappers; side effects consume those final facts and return a sealed dense enum table; execution order, TLA, and dynamic exports are sealed compact facts; and cycle diagnostics use deterministic first-discovery order. The complete thirteen-pass state has passed broad validation and formal review.
- Phase 4 is complete through the twenty-second pass. It covers resolved-export collection and finalization, serial import binding, sparse CJS routing, member resolution, entry-root collection, synthetic statements, reference analysis, and bounded cross-module optimization. Binding retains its existing immediate serial commit semantics; the analysis/commit split was rejected for this production cutover.
- The validation corpus covers pass-harness runtime and UI contracts, source-inventory enforcement, all-feature Rust and strict Clippy matrices, real WASM and Node binding builds, Node and Rollup suites, deterministic development digests, fresh diagnostic-order processes, and the production pass trace. Lazy normalization additionally carries its identity, transform-AST, JSON, CJS, HMR, arbitrary-name, and renderer fixtures.
- Phase 5 is complete. H consumes entries and returns retained entries, inclusion results, normalized runtime requirements, used-symbol continuations, enum presence, and narrow module patches while preserving phase-one versus runtime-phase scratch lifetimes. G consumes dependency and helper drafts, performs immutable parallel analysis plus serial physical commit, and returns finalized dependency data. The one-shot adapter validates final layouts, preserves deferred patch order, constructs metadata during one physical module traversal, projects sparse facts, and constructs the unchanged output. Obsolete Link-only inclusion wrappers and dependency algorithms are removed. The complete correctness-reviewed structure is the only timing and RSS acceptance candidate.

## Related

- [design.md](./design.md) — contract rationale, enforcement boundary, and rejected alternatives
- [baseline.md](./baseline.md) — reproducible link behavior, timing, digest, trace, and process-RSS contract
- [module side effects](../linking/module-side-effects/implementation.md) — exact serial side-effect algorithm, typed inputs, and sealed output lifecycle
- [resolved exports](../linking/resolved-exports/implementation.md) — raw collection, binding barrier, canonical finalization, and delayed owned no-clone projection
- [cross-module optimization](../linking/cross-module-optimization/implementation.md) — P's exact ownership manifest, bounded rounds, parallel analysis, deterministic commit, and compatibility constraints
- `../linking/` — current link implementation notes and behavior constraints
