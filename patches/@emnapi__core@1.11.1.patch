diff --git a/dist/emnapi-core.cjs.js b/dist/emnapi-core.cjs.js
index 40d09ef4d21ea4f54cc4d80cc0616375711bee15..d40ddc560a4dd514f8cde3737b6c9bee1f1c6c3c 100644
--- a/dist/emnapi-core.cjs.js
+++ b/dist/emnapi-core.cjs.js
@@ -1440,12 +1440,11 @@ function createNapiModule(options) {
                 var has_more = true;
                 var iterations_left = 1000;
                 var dispatchStateAddress = func + emnapiTSFN.offset.dispatch_state;
-                var ui32a = new Uint32Array(emnapiTSFN.ensureBufferFor(dispatchStateAddress + 4));
                 var index = dispatchStateAddress >>> 2;
                 while (has_more && --iterations_left !== 0) {
-                    Atomics.store(ui32a, index, 1);
+                    Atomics.store(new Uint32Array(emnapiTSFN.ensureBufferFor(dispatchStateAddress + 4)), index, 1);
                     has_more = emnapiTSFN.dispatchOne(func);
-                    if (Atomics.exchange(ui32a, index, 0) !== 1) {
+                    if (Atomics.exchange(new Uint32Array(emnapiTSFN.ensureBufferFor(dispatchStateAddress + 4)), index, 0) !== 1) {
                         has_more = true;
                     }
                 }
@@ -1456,21 +1455,22 @@ function createNapiModule(options) {
             enqueue: function (func) {
                 var pending = func + emnapiTSFN.offset.async_pending;
                 var scheduled = func + emnapiTSFN.offset.async_u_fd;
-                var i32a = new Int32Array(emnapiTSFN.ensureBufferFor(Math.max(pending, scheduled) + 4));
-                if (Atomics.exchange(i32a, scheduled >>> 2, 1) !== 0) {
+                var end = Math.max(pending, scheduled) + 4;
+                var state = function () { return new Int32Array(emnapiTSFN.ensureBufferFor(end)); };
+                if (Atomics.exchange(state(), scheduled >>> 2, 1) !== 0) {
                     return;
                 }
                 emnapiCtx.feature.setImmediate(function () {
                     if (!emnapiTSFN._liveSet.has(func)) {
                         return;
                     }
-                    if (Atomics.load(i32a, pending >>> 2) === 0) {
-                        Atomics.store(i32a, scheduled >>> 2, 0);
+                    if (Atomics.load(state(), pending >>> 2) === 0) {
+                        Atomics.store(state(), scheduled >>> 2, 0);
                         return;
                     }
                     emnapiCtx.feature.setImmediate(function () {
                         try {
-                            if (Atomics.exchange(i32a, pending >>> 2, 0) === 0) {
+                            if (Atomics.exchange(state(), pending >>> 2, 0) === 0) {
                                 return;
                             }
                             if (!emnapiTSFN._liveSet.has(func)) {
@@ -1480,8 +1480,8 @@ function createNapiModule(options) {
                         }
                         finally {
                             if (emnapiTSFN._liveSet.has(func)) {
-                                Atomics.store(i32a, scheduled >>> 2, 0);
-                                if (Atomics.load(i32a, pending >>> 2) !== 0) {
+                                Atomics.store(state(), scheduled >>> 2, 0);
+                                if (Atomics.load(state(), pending >>> 2) !== 0) {
                                     emnapiTSFN.enqueue(func);
                                 }
                             }
@@ -5677,7 +5677,7 @@ function createNapiModule(options) {
                 var ret = v8func.apply(v8recv, args);
                 if (result) {
                     v = envObject.ensureHandleId(ret);
-                    HEAP_DATA_VIEW.setUint32(result, v, true);
+                    new DataView(wasmMemory.buffer).setUint32(result, v, true);
                 }
                 return envObject.clearLastError();
             }
@@ -5736,7 +5736,7 @@ function createNapiModule(options) {
                 }
                 if (result) {
                     v = envObject.ensureHandleId(ret);
-                    HEAP_DATA_VIEW.setUint32(result, v, true);
+                    new DataView(wasmMemory.buffer).setUint32(result, v, true);
                 }
                 return envObject.getReturnStatus();
             }
diff --git a/dist/emnapi-core.esm-bundler.js b/dist/emnapi-core.esm-bundler.js
index d914d4fe1f1c8d12ef095d4cadc787f48e3f7d49..977eabaf81d6661b297b43a8cd823db2c0f3bcb6 100644
--- a/dist/emnapi-core.esm-bundler.js
+++ b/dist/emnapi-core.esm-bundler.js
@@ -1646,12 +1646,11 @@ function createNapiModule(options) {
                 var has_more = true;
                 var iterations_left = 1000;
                 var dispatchStateAddress = func + emnapiTSFN.offset.dispatch_state;
-                var ui32a = new Uint32Array(emnapiTSFN.ensureBufferFor(dispatchStateAddress + 4));
                 var index = dispatchStateAddress >>> 2;
                 while (has_more && --iterations_left !== 0) {
-                    Atomics.store(ui32a, index, 1);
+                    Atomics.store(new Uint32Array(emnapiTSFN.ensureBufferFor(dispatchStateAddress + 4)), index, 1);
                     has_more = emnapiTSFN.dispatchOne(func);
-                    if (Atomics.exchange(ui32a, index, 0) !== 1) {
+                    if (Atomics.exchange(new Uint32Array(emnapiTSFN.ensureBufferFor(dispatchStateAddress + 4)), index, 0) !== 1) {
                         has_more = true;
                     }
                 }
@@ -1666,8 +1665,9 @@ function createNapiModule(options) {
                 // `scheduled` prevents queueing the same main-thread drain chain more than
                 // once while a previous wakeup is still in flight.
                 var scheduled = func + emnapiTSFN.offset.async_u_fd;
-                var i32a = new Int32Array(emnapiTSFN.ensureBufferFor(Math.max(pending, scheduled) + 4));
-                if (Atomics.exchange(i32a, scheduled >>> 2, 1) !== 0) {
+                var end = Math.max(pending, scheduled) + 4;
+                var state = function () { return new Int32Array(emnapiTSFN.ensureBufferFor(end)); };
+                if (Atomics.exchange(state(), scheduled >>> 2, 1) !== 0) {
                     return;
                 }
                 // Match uv_async_send-style coalescing in JS: the first turn represents
@@ -1678,15 +1678,15 @@ function createNapiModule(options) {
                     if (!emnapiTSFN._liveSet.has(func)) {
                         return;
                     }
-                    if (Atomics.load(i32a, pending >>> 2) === 0) {
-                        Atomics.store(i32a, scheduled >>> 2, 0);
+                    if (Atomics.load(state(), pending >>> 2) === 0) {
+                        Atomics.store(state(), scheduled >>> 2, 0);
                         return;
                     }
                     emnapiCtx.feature.setImmediate(function () {
                         try {
                             // Consume the coalesced wakeup once, then let dispatch() observe any
                             // queue mutations through dispatch_state like the C implementation.
-                            if (Atomics.exchange(i32a, pending >>> 2, 0) === 0) {
+                            if (Atomics.exchange(state(), pending >>> 2, 0) === 0) {
                                 return;
                             }
                             // After destroy(), the func address is freed.  Skip dispatch
@@ -1700,8 +1700,8 @@ function createNapiModule(options) {
                             // Allow a later wakeup to schedule a new drain chain. If another
                             // worker-thread send raced with this drain, enqueue one more turn.
                             if (emnapiTSFN._liveSet.has(func)) {
-                                Atomics.store(i32a, scheduled >>> 2, 0);
-                                if (Atomics.load(i32a, pending >>> 2) !== 0) {
+                                Atomics.store(state(), scheduled >>> 2, 0);
+                                if (Atomics.load(state(), pending >>> 2) !== 0) {
                                     emnapiTSFN.enqueue(func);
                                 }
                             }
@@ -6396,7 +6396,7 @@ function createNapiModule(options) {
                 var ret = v8func.apply(v8recv, args);
                 if (result) {
                     v = envObject.ensureHandleId(ret);
-                    HEAP_DATA_VIEW.setUint32(result, v, true);
+                    new DataView(wasmMemory.buffer).setUint32(result, v, true);
                 }
                 return envObject.clearLastError();
             }
@@ -6458,7 +6458,7 @@ function createNapiModule(options) {
                 }
                 if (result) {
                     v = envObject.ensureHandleId(ret);
-                    HEAP_DATA_VIEW.setUint32(result, v, true);
+                    new DataView(wasmMemory.buffer).setUint32(result, v, true);
                 }
                 return envObject.getReturnStatus();
             }
diff --git a/dist/emnapi-core.mjs b/dist/emnapi-core.mjs
index a03582b92695d64fb4353060e51c61b726c6556d..d738a2e3df0eee3535fdedcd013a1ddd6d869f83 100644
--- a/dist/emnapi-core.mjs
+++ b/dist/emnapi-core.mjs
@@ -1441,12 +1441,11 @@ function createNapiModule(options) {
                 var has_more = true;
                 var iterations_left = 1000;
                 var dispatchStateAddress = func + emnapiTSFN.offset.dispatch_state;
-                var ui32a = new Uint32Array(emnapiTSFN.ensureBufferFor(dispatchStateAddress + 4));
                 var index = dispatchStateAddress >>> 2;
                 while (has_more && --iterations_left !== 0) {
-                    Atomics.store(ui32a, index, 1);
+                    Atomics.store(new Uint32Array(emnapiTSFN.ensureBufferFor(dispatchStateAddress + 4)), index, 1);
                     has_more = emnapiTSFN.dispatchOne(func);
-                    if (Atomics.exchange(ui32a, index, 0) !== 1) {
+                    if (Atomics.exchange(new Uint32Array(emnapiTSFN.ensureBufferFor(dispatchStateAddress + 4)), index, 0) !== 1) {
                         has_more = true;
                     }
                 }
@@ -1457,21 +1456,22 @@ function createNapiModule(options) {
             enqueue: function (func) {
                 var pending = func + emnapiTSFN.offset.async_pending;
                 var scheduled = func + emnapiTSFN.offset.async_u_fd;
-                var i32a = new Int32Array(emnapiTSFN.ensureBufferFor(Math.max(pending, scheduled) + 4));
-                if (Atomics.exchange(i32a, scheduled >>> 2, 1) !== 0) {
+                var end = Math.max(pending, scheduled) + 4;
+                var state = function () { return new Int32Array(emnapiTSFN.ensureBufferFor(end)); };
+                if (Atomics.exchange(state(), scheduled >>> 2, 1) !== 0) {
                     return;
                 }
                 emnapiCtx.feature.setImmediate(function () {
                     if (!emnapiTSFN._liveSet.has(func)) {
                         return;
                     }
-                    if (Atomics.load(i32a, pending >>> 2) === 0) {
-                        Atomics.store(i32a, scheduled >>> 2, 0);
+                    if (Atomics.load(state(), pending >>> 2) === 0) {
+                        Atomics.store(state(), scheduled >>> 2, 0);
                         return;
                     }
                     emnapiCtx.feature.setImmediate(function () {
                         try {
-                            if (Atomics.exchange(i32a, pending >>> 2, 0) === 0) {
+                            if (Atomics.exchange(state(), pending >>> 2, 0) === 0) {
                                 return;
                             }
                             if (!emnapiTSFN._liveSet.has(func)) {
@@ -1481,8 +1481,8 @@ function createNapiModule(options) {
                         }
                         finally {
                             if (emnapiTSFN._liveSet.has(func)) {
-                                Atomics.store(i32a, scheduled >>> 2, 0);
-                                if (Atomics.load(i32a, pending >>> 2) !== 0) {
+                                Atomics.store(state(), scheduled >>> 2, 0);
+                                if (Atomics.load(state(), pending >>> 2) !== 0) {
                                     emnapiTSFN.enqueue(func);
                                 }
                             }
@@ -5678,7 +5678,7 @@ function createNapiModule(options) {
                 var ret = v8func.apply(v8recv, args);
                 if (result) {
                     v = envObject.ensureHandleId(ret);
-                    HEAP_DATA_VIEW.setUint32(result, v, true);
+                    new DataView(wasmMemory.buffer).setUint32(result, v, true);
                 }
                 return envObject.clearLastError();
             }
@@ -5737,7 +5737,7 @@ function createNapiModule(options) {
                 }
                 if (result) {
                     v = envObject.ensureHandleId(ret);
-                    HEAP_DATA_VIEW.setUint32(result, v, true);
+                    new DataView(wasmMemory.buffer).setUint32(result, v, true);
                 }
                 return envObject.getReturnStatus();
             }
